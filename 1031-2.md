### 1031-2 修复"序列阶段标记占用但中间件过滤后未执行"导致的误占问题

#### 问题背景
- 从export文件统计：实际执行了175个点
- 从日志统计：203个槽位被标记为占用（occupied_ids=203）
- **差值：203 - 175 = 28个槽位**
- 这28个槽位被标记为占用，但从未实际执行，导致后续月份无法选择这些槽位

#### 问题根源
在 `step_phase` 方法中存在逻辑错误：
- **执行阶段**（第1548行）：使用 `processed_sequence`（经过中间件处理后的序列）
- **标记占用阶段**（第1554行）：使用原始 `sequence`（未经过中间件处理）

**导致的问题：**
1. 原始序列选择了某些槽位
2. 中间件处理（如 `trim_to_max_len`、`drop_late`、`budget.shared_ledger` 等）过滤掉了一些动作
3. 执行时只执行了 `processed_sequence` 中的动作（175个点）
4. 但标记占用时使用的是原始 `sequence`，把所有槽位都标记为占用了（203个槽位）
5. 差值28个槽位 = 被中间件过滤掉但被原始序列标记占用的槽位

#### 代码改动
- **文件**：`envs/v5_0/city_env.py`
- **位置**：`step_phase` 方法，约第1554行
- **变更前**：
  ```python
  self._update_occupied_slots_for_agent(agent, sequence)  # 使用原始sequence
  ```
- **变更后**：
  ```python
  self._update_occupied_slots_for_agent(agent, processed_sequence)  # 使用processed_sequence
  ```

#### 修复原理
- 确保标记占用时使用的序列与执行时使用的序列一致
- 只有实际执行的槽位才会被标记为占用
- 被中间件过滤掉的槽位不会被错误标记，可以在后续月份被选择

#### 预期效果
- `occupied_ids` 应该与实际执行的槽位数一致（或接近）
- `non_exec_ids`（未执行但占用）应该显著减少
- 后续月份应该能够正常选择被中间件过滤掉的槽位
- 验证：重新运行训练后，`occupied_ids` - 实际执行点数应该接近0

#### 相关日志
- `[OCCUPY_FROM_SEQUENCE]`：标记来自序列阶段的占用
- `[OCCUPY_FROM_EXEC]`：标记来自执行阶段的占用
- `[OCCUPIED_SUMMARY]`：月度汇总，显示 `occupied_ids`、`built_ids`、`non_exec_ids`

#### 备注
- 此问题与1030-2.md中记录的"未执行也被占用"问题相关
- 修复后应该能显著减少 `non_exec_ids` 的数量
- 需要验证修复效果：重新运行训练并检查 `occupied_ids` 与 export 文件中的实际执行点数是否一致

















