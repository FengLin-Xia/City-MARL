### 1030-1 外部因素优先的动作多样性整改方案（记录）

#### 背景
- 新一轮运行中，EDU在月29出现策略坍塌：entropy≈0.54，类型分布近δ（{1:1.0}）。
- 之前What-if显示基础项对0/1/2全为负值且差距大（0>-605，1≈-1015，2≈-1600），在空间项接近时天然偏向低成本；但本轮坍塌幅度异常，判断为“训练与采样外部因素”叠加。

#### 成因拆解（非奖励本体）
- 采样与熵驱动：温度与stop_bias组合可能降低“有效温度”；熵系数偏低导致策略头快速推到极端。
- 多动作机制：掩码更新、去重、topP与max_k互动会放大早期偏好，形成路径依赖。
- 候选与约束：河流/合法性/Hub限制收窄候选，空间项区分度不足时，基础项差异主导选择。
- 表征可分性：_encode_state对规模区分不敏感时，梯度偏向更易学的低成本/单一类型。

#### 本次优先解决的“外部因素”与调整项
1) 提升探索力度（立即生效目标）
   - 上调 `mappo.ppo.entropy_coef`（+0.3）
   - 上调 `multi_action.temperature`（+1.0）
   - 降低 `multi_action.stop_bias` 的绝对值（趋近0）
   - 如需，可小幅上调 `mappo.exploration.temperature`（+0.5）

2) 运行短训复核（3–5 episodes）
   - 观察指标：
     - EDU/IND/COUNCIL 的 `entropy_sum` 是否回到健康区间（>10为佳）
     - `[TYPE_PROBS_POINT]` 最高概率是否<0.8，防止单一动作垄断
     - `[CANDIDATE_STATS]` 类型可用性是否长期失衡

3) 保留奖励设计不动（阶段性结论）
   - 暂不动基础reward/成本与size_bonus等；先排除外部因素对探索性的抑制，再评估奖励结构。

#### 预期验证口径
- 若短训后：
  - 熵值回升且类型概率不再极端 → 外部因素为主因，后续再评估奖励结构是否需微调。
  - 熵值与类型概率仍极端 → 继续排查候选生成/掩码/state编码差异性，最后再回到奖励本体。

#### 执行计划
- 修改 `configs/city_config_v5_0.json`：
  - `mappo.ppo.entropy_coef`: 0.5 → 0.8
  - `multi_action.temperature`: 3.5 → 4.5
  - `multi_action.stop_bias`: -0.2 → -0.05
  - `mappo.exploration.temperature`: 3.5 → 4.0（如需）
- 运行 3–5 episodes 短训，提取日志并评估上述指标。



