# 建筑等级限制Bug修复记录 (2025-01-27)

## 问题描述
用户观察到IND智能体违反建筑等级限制，在 `building_level=3` 的槽位上建造了需要 `building_level=5` 的建筑（动作5，IND_L）。

## 问题分析

### 错误逻辑
代码在 `logic/v5_enumeration.py` 中错误地使用 `footprint_size`（占地面积）来检查建筑等级限制：

```python
# 错误的实现
if "L" in desc:
    footprint_size = 4  # 4个槽位
# ...
if slot.building_level < footprint_size:  # 错误：用占地面积检查等级
    continue
```

### 问题根源
- **动作5 (IND_L)**：`footprint_size = 4`（4个槽位）
- **槽位等级3**：只能建 `building_level >= 3` 的建筑
- **错误检查**：`if slot.building_level < footprint_size` → `if 3 < 4` → `True`，所以被过滤掉了
- **实际需求**：IND_L 需要 `building_level >= 5`，但槽位只有等级3

## 修复方案

### 修改文件
`logic/v5_enumeration.py`

### 修改内容

#### 1. 单槽位动作检查（`_get_valid_types_for_point` 方法）
```python
# 修改前
if "S" in desc:
    footprint_size = 1
elif "M" in desc:
    footprint_size = 2
elif "L" in desc:
    footprint_size = 4
else:
    footprint_size = 1

if slot.building_level < footprint_size:  # 错误
    continue

# 修改后
if "S" in desc:
    footprint_size = 1
    required_level = 3  # S型建筑需要等级3
elif "M" in desc:
    footprint_size = 2
    required_level = 4  # M型建筑需要等级4
elif "L" in desc:
    footprint_size = 4
    required_level = 5  # L型建筑需要等级5
else:
    footprint_size = 1
    required_level = 3

if slot.building_level < required_level:  # 正确
    continue
```

#### 2. 多槽位动作检查（`_enumerate_positions` 方法）
```python
# 修改前
if should_check_level and slot.building_level < footprint_size:
    continue

# 修改后
if should_check_level:
    # 根据动作类型确定建筑等级要求
    desc = action_params.get("desc", "")
    if "S" in desc:
        required_level = 3
    elif "M" in desc:
        required_level = 4
    elif "L" in desc:
        required_level = 5
    else:
        required_level = 3
    
    if slot.building_level < required_level:
        continue
```

## 修复后的正确逻辑

### 建筑等级要求
- **动作0/3/6 (EDU_S/IND_S/COUNCIL_A)**：需要 `building_level >= 3`
- **动作1/4/7 (EDU_M/IND_M/COUNCIL_B)**：需要 `building_level >= 4`
- **动作2/5/8 (EDU_L/IND_L/COUNCIL_C)**：需要 `building_level >= 5`

### 槽位等级限制
- **等级3槽位**：只能建 S 型建筑
- **等级4槽位**：可以建 S、M 型建筑
- **等级5槽位**：可以建 S、M、L 型建筑

### 检查逻辑
```python
if slot.building_level < required_level:
    continue  # 跳过不符合等级要求的动作
```

## 影响范围

### 配置影响
- 建筑等级限制配置 `building_level_restriction.apply_to_agents: ["IND"]` 现在正确生效
- IND智能体不会再在低等级槽位上建造高等级建筑

### 动作多样性
- 修复后，IND智能体的可选动作范围会更合理
- 可能减少重复槽位选择问题（因为可选槽位更少）

## 测试建议

1. **验证等级限制**：确认IND智能体不再在等级3槽位上建造IND_L
2. **检查动作分布**：观察修复后的动作选择分布
3. **监控槽位冲突**：确认重复槽位选择问题是否得到改善

## 相关配置

当前建筑等级限制配置：
```json
"building_level_restriction": {
    "enabled": true,
    "apply_to_agents": ["IND"]
}
```

这意味着：
- IND智能体受建筑等级限制
- EDU和COUNCIL智能体不受限制
- 限制规则：`slot.building_level >= required_level`

## 后续发现的问题

### 配置缺失问题
在调试过程中发现，虽然 `1025-4.md` 记录显示之前添加过 `building_level_restriction` 配置，但在当前配置文件中该配置缺失。

**可能原因**：
1. 在后续的参数调整过程中被意外删除
2. 使用了旧版本的配置文件
3. 配置位置错误

**解决方案**：
重新添加了缺失的配置到 `configs/city_config_v5_0.json`：
```json
"constraints": {
    "building_level_restriction": {
        "enabled": true,
        "apply_to_agents": ["IND"]
    }
}
```

### 调试过程
1. **添加调试代码**：在枚举器中添加了调试输出，但没有看到相关日志
2. **检查日志**：发现大量 `action_id=5` 的执行记录
3. **验证槽位等级**：确认槽位62等级为3，但动作5仍在执行
4. **检查配置**：发现配置文件中缺少 `building_level_restriction` 配置
5. **重新添加配置**：修复了配置缺失问题

### 最终状态
- 配置已恢复
- 代码逻辑正确
- IND智能体现在应该正确遵守建筑等级限制
