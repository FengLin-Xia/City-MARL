# 1031-4：31个槽位问题深度分析

## 问题概述

**核心问题：31个槽位被EXEC标记了（真正执行了），但不在export文件中**

### 数据统计

- **occupied_ids = 203个**：所有被标记为占用的槽位（SEQUENCE + EXEC）
- **export文件 = 172个**：写入export文件的槽位
- **EXEC标记 = 85个**：真正执行的槽位
- **差值 = 203 - 172 = 31个**：被标记但未写入export文件的槽位

### 31个槽位的标记情况

- **没有SEQUENCE标记的槽位：4个**
  - slot_0: EXEC标记36次, SEQUENCE标记0次
  - slot_1: EXEC标记36次, SEQUENCE标记0次
  - slot_64: EXEC标记18次, SEQUENCE标记0次
  - slot_70: EXEC标记18次, SEQUENCE标记0次

- **有SEQUENCE标记的槽位：27个**
  - slot_11: SEQUENCE标记3次, EXEC标记18次
  - slot_12: SEQUENCE标记4次, EXEC标记18次
  - slot_33: SEQUENCE标记5次, EXEC标记12次
  - ...等等

## EXEC功能说明

### EXEC是什么

**EXEC** 代表 **执行阶段（Execution Stage）**，是动作执行的最后一个阶段，负责**实际执行动作并建造建筑**。

### EXEC的职责

1. **实际执行动作**
   - 调用 `_execute_action_atomic` 或 `_execute_action_legacy`
   - 从候选快照中获取动作候选
   - 实际建造建筑（如果候选存在）

2. **标记槽位为已占用**
   - 将槽位标记为已占用（`_mark_slot_id_occupied`）
   - 添加到 `occupied_slots` 和 `global_occupied_slots`
   - 记录到 `_built_slot_ids`（表示真正已建设）

3. **更新预算和奖励**
   - 扣除建造成本
   - 计算奖励
   - 更新预算

### EXEC vs SEQUENCE

- **SEQUENCE阶段（选择阶段）**
  - 时机：动作序列生成后，中间件处理前
  - 功能：根据RL选择的动作序列，**预先标记**槽位为占用
  - 位置：`_update_occupied_slots_for_agent` 方法中
  - 日志：`[OCCUPY_FROM_SEQUENCE]`
  - 问题：可能标记了未执行的槽位（如果中间件过滤了动作）

- **EXEC阶段（执行阶段）**
  - 时机：中间件处理后，实际执行动作时
  - 功能：**真正执行**动作并建造建筑
  - 位置：`_execute_action_atomic` 和 `_execute_action_legacy` 方法中
  - 日志：`[OCCUPY_FROM_EXEC]`
  - 准确性：只标记**真正执行**的槽位

## EXEC与processed_sequence的关系

### 直接关系

**EXEC执行的就是processed_sequence！**

### 代码证据

**在`step_phase`方法中（第1548行）：**

```python
# 应用中间件处理序列
processed_sequence = self._apply_middleware(agent, sequence)

# 执行智能体 {agent} 的动作序列: {processed_sequence}
reward, reward_terms = self._execute_agent_sequence(agent, processed_sequence)
```

**结论：**
1. EXEC执行的是 `processed_sequence`
2. EXEC标记的槽位 = `processed_sequence` 中的槽位
3. EXEC标记的槽位都是真正执行的槽位

## action.meta问题分析

### 关键发现

**有两个不同的slots数据来源：**

1. **action.meta中的slots**（在`rl_selector.py`第620行创建AtomicAction时）：
   ```python
   slots = cand_idx.point_to_slots.get(point_id, [])
   meta={"action_id": action_type, "point_id": point_id, "slots": slots}
   ```
   - 从`cand_idx.point_to_slots`获取

2. **EXEC中的slots**（在`city_env.py`第615行执行时）：
   ```python
   cand = self._get_candidate_from_snapshot(agent, action_id)
   for slot_id in cand.meta.get("slots", []):  # 从候选快照获取
   ```
   - 从候选快照（`cand.meta.get("slots", [])`）获取

3. **SEQUENCE中的slots**（在`city_env.py`第794行标记时）：
   ```python
   for action in sequence.actions:  # sequence是processed_sequence
       slots = action.meta.get("slots", [])  # 从action.meta获取
   ```
   - 从`action.meta.get("slots", [])`获取

4. **slot_positions中的slots**（在`city_env.py`第876行生成时）：
   ```python
   for action in sequence.actions:  # sequence是原始sequence（第1595行）
       point_id = cand_idx.points[action.point]
       slots = cand_idx.point_to_slots.get(point_id, [])  # 从cand_idx获取
   ```
   - 从`cand_idx.point_to_slots`获取

### 问题

**这两个来源可能不一致！**

- action.meta中的slots = `cand_idx.point_to_slots.get(point_id, [])`
- EXEC中的slots = 候选快照（`cand.meta.get("slots", [])`）

## 逻辑纠正

### 实际数据

```
EXEC标记的唯一槽位数量: 85个
export文件中的唯一坐标数量: 172个
差值: -87个（EXEC < export）
```

### 重新理解

- **export文件包含EXEC未标记的槽位**
  - export文件中有172个唯一坐标
  - EXEC标记了85个唯一槽位
  - 差值：172 - 85 = 87个槽位在export文件中但EXEC未标记

- **EXEC标记了但不在export文件中的槽位**
  - 有31个槽位EXEC标记了但不在export文件中
  - 这些槽位真正执行了，但没有写入export文件

- **export文件的生成逻辑**
  - export文件的数据来源是**StepLog.slot_positions**
  - 而`slot_positions`的生成使用的是**原始sequence**（不是processed_sequence）
  - 所以export文件包含的是原始sequence中的槽位（可能未执行）

## 两个可能的原因深度剖析

### 原因1：slot_positions使用了原始sequence，而不是processed_sequence

#### 代码位置

```python
# envs/v5_0/city_env.py

# 第1542行：生成processed_sequence（经过中间件过滤）
processed_sequence = self._apply_middleware(agent, sequence)

# 第1548行：EXEC执行processed_sequence
reward, reward_terms = self._execute_agent_sequence(agent, processed_sequence)

# 第1555行：SEQUENCE标记使用processed_sequence ✅
self._update_occupied_slots_for_agent(agent, processed_sequence)

# 第1595行：slot_positions使用原始sequence ❌
sequence = phase_sequences[agent]  # ❌ 这是原始sequence
slot_positions = self._build_slot_positions_from_snapshot(agent, sequence)
```

#### 问题分析

**如果processed_sequence是原始sequence经过中间件过滤的结果：**
- processed_sequence应该是原始sequence的**子集**
- processed_sequence中的槽位应该都在原始sequence中
- 理论上，EXEC执行的槽位应该都在slot_positions中

**但实际上有31个槽位不在slot_positions中，可能的原因：**

1. **中间件过滤逻辑复杂**
   - 中间件可能不是简单地删除action，而是创建新的Sequence
   - 新的Sequence中的action可能与原始sequence中的action不完全对应
   - 导致processed_sequence中的某些action不在原始sequence中（虽然理论上不应该发生）

2. **slot_positions生成时机问题**
   - slot_positions在生成时使用的是原始sequence
   - 但此时processed_sequence已经生成并执行
   - 如果processed_sequence中有新的action（不是原始sequence的子集），就不会出现在slot_positions中

#### 中间件过滤机制

**从中间件代码看（`candidate_range.py`第50-55行）：**
```python
# 过滤动作，只保留在候选范围内的动作
filtered_actions = []
for action_id in seq.actions:
    if self._is_action_in_range(action_id, available_slots, state):
        filtered_actions.append(action_id)

return Sequence(agent=seq.agent, actions=filtered_actions)
```

**中间件是过滤action，不是修改action：**
- 中间件从原始sequence中过滤掉不符合条件的action
- 返回新的Sequence，只包含符合条件的action
- 新的Sequence中的action应该都是原始sequence中的action

**所以理论上processed_sequence应该是原始sequence的子集，但为什么会有31个槽位不在slot_positions中？**

### 原因2：slot_positions从cand_idx获取，可能与action.meta不一致

#### 代码位置

```python
# envs/v5_0/city_env.py 第876行

# slot_positions生成时从cand_idx获取
for action in sequence.actions:  # sequence是原始sequence
    point_id = cand_idx.points[action.point]
    slots = cand_idx.point_to_slots.get(point_id, [])  # 从cand_idx获取
```

```python
# envs/v5_0/city_env.py 第615行

# EXEC执行时从候选快照获取
cand = self._get_candidate_from_snapshot(agent, action_id)
for slot_id in cand.meta.get("slots", []):  # 从候选快照获取
```

```python
# envs/v5_0/city_env.py 第794行

# SEQUENCE标记时从action.meta获取
for action in sequence.actions:  # sequence是processed_sequence
    slots = action.meta.get("slots", [])  # 从action.meta获取
```

#### 问题分析

**有三个不同的slots数据来源：**

1. **action.meta中的slots** = `cand_idx.point_to_slots.get(point_id, [])`（在创建AtomicAction时）
2. **EXEC中的slots** = 候选快照（`cand.meta.get("slots", [])`）
3. **slot_positions中的slots** = `cand_idx.point_to_slots.get(point_id, [])`（在生成slot_positions时）

**关键问题：**
- `cand_idx.point_to_slots`和候选快照中的`cand.meta.get("slots", [])`可能不一致
- 如果`cand_idx.point_to_slots`中没有某个point_id，但候选快照中有对应的cand
- EXEC能从候选快照中获取到slots并执行
- 但slot_positions从`cand_idx.point_to_slots`获取不到，就不会出现在export文件中

#### 可能的场景

**场景1：cand_idx在中间件过滤后被更新**
- 中间件过滤后，某些point被过滤掉
- `cand_idx`可能被更新，移除了这些point
- 但候选快照（`_last_candidates`）中还保留着这些cand的信息
- 导致EXEC能从候选快照中找到，但slot_positions从`cand_idx`中找不到

**场景2：action.meta中的slots为空，但候选快照中有**
- 如果`cand_idx.point_to_slots.get(point_id, [])`返回空列表
- action.meta.slots就是空列表
- SEQUENCE阶段就不会标记这些槽位（从action.meta获取不到）
- 但EXEC阶段从候选快照中能找到并标记
- slot_positions也获取不到（从`cand_idx.point_to_slots`获取不到）

**场景3：中间件过滤后，processed_sequence中的action不在原始sequence中**
- 虽然理论上不应该发生，但可能由于中间件实现问题
- processed_sequence中有新的action（不是原始sequence的子集）
- 这些action在EXEC中执行，但不在原始sequence的slot_positions中

## 深度分析这两个原因

### 原因1的深度剖析：slot_positions使用原始sequence的问题

#### 执行流程对比

```
正常流程：
1. RL选择动作序列 → sequence（原始序列，包含action.meta）
2. 应用中间件处理 → processed_sequence（过滤后的序列，action.meta保持不变）
3. EXEC执行 → _execute_agent_sequence(agent, processed_sequence)
4. SEQUENCE标记 → _update_occupied_slots_for_agent(agent, processed_sequence)（从action.meta获取）
5. slot_positions生成 → _build_slot_positions_from_snapshot(agent, sequence)（使用原始sequence）

问题流程：
1. processed_sequence中的action应该都在原始sequence中
2. 但如果中间件过滤后，processed_sequence中有action，但原始sequence中对应action的point_id在cand_idx中找不到
3. slot_positions生成时从cand_idx获取不到，就不会出现在export文件中
```

#### 具体检查点

需要检查：
1. **中间件过滤后，processed_sequence中的action是否都在原始sequence中？**
2. **原始sequence中的action是否都有对应的point_id在cand_idx中？**
3. **cand_idx在中间件过滤后是否会被更新？**

### 原因2的深度剖析：cand_idx与候选快照不一致的问题

#### 数据流追踪

```
创建AtomicAction时（rl_selector.py第620行）：
- slots = cand_idx.point_to_slots.get(point_id, [])
- action.meta = {"slots": slots}
- 如果point_id不在cand_idx.point_to_slots中，slots = []

EXEC执行时（city_env.py第615行）：
- cand = self._get_candidate_from_snapshot(agent, action_id)
- slots = cand.meta.get("slots", [])
- 从候选快照获取，可能有值

slot_positions生成时（city_env.py第876行）：
- slots = cand_idx.point_to_slots.get(point_id, [])
- 从cand_idx获取，如果point_id不在，slots = []
```

#### 具体检查点

需要检查：
1. **cand_idx.point_to_slots和候选快照中的cand.meta["slots"]是否一致？**
2. **如果中间件过滤后，cand_idx是否会被更新？**
3. **候选快照（_last_candidates）是否在中间件过滤后保持不变？**

## 下一步验证方案

### 验证方案1：检查中间件过滤后processed_sequence的变化

1. 在中间件过滤前后记录sequence和processed_sequence
2. 检查processed_sequence中的action是否都在原始sequence中
3. 检查processed_sequence中的action的action.meta是否保持一致

### 验证方案2：检查cand_idx与候选快照的一致性

1. 在生成slot_positions时，同时从cand_idx和候选快照获取slots
2. 对比两者的差异
3. 找出为什么有些槽位在候选快照中，但不在cand_idx中

### 验证方案3：检查slot_positions生成逻辑

1. 检查_build_slot_positions_from_snapshot是否应该使用processed_sequence
2. 检查是否应该从action.meta获取slots，而不是从cand_idx获取
3. 检查是否应该从候选快照获取slots，与EXEC保持一致

## 深度剖析两个可能的原因

### 原因1：slot_positions使用了原始sequence，而不是processed_sequence

#### 代码逻辑分析

**执行流程：**
```python
# 第1532行：获取原始sequence
sequence = phase_sequences[agent]

# 第1542行：生成processed_sequence（经过中间件过滤）
processed_sequence = self._apply_middleware(agent, sequence)

# 第1548行：EXEC执行processed_sequence
reward, reward_terms = self._execute_agent_sequence(agent, processed_sequence)

# 第1555行：SEQUENCE标记使用processed_sequence ✅
self._update_occupied_slots_for_agent(agent, processed_sequence)

# 第1595行：slot_positions使用原始sequence ❌
sequence = phase_sequences[agent]  # ❌ 这是原始sequence
slot_positions = self._build_slot_positions_from_snapshot(agent, sequence)
```

#### 中间件过滤机制

**从中间件代码看（`candidate_range.py`）：**
```python
# 过滤动作，只保留在候选范围内的动作
filtered_actions = []
for action_id in seq.actions:
    if self._is_action_in_range(action_id, available_slots, state):
        filtered_actions.append(action_id)

return Sequence(agent=seq.agent, actions=filtered_actions)
```

**中间件是过滤action，不是修改action：**
- 中间件从原始sequence中过滤掉不符合条件的action
- 返回新的Sequence，只包含符合条件的action
- 理论上processed_sequence应该是原始sequence的子集

**但为什么会有31个槽位不在slot_positions中？**

#### 可能的问题点1：action.point索引超出范围

**在`_build_slot_positions_from_snapshot`中（第874行）：**
```python
for action in sequence.actions:  # sequence是原始sequence
    if hasattr(action, 'point') and action.point < len(cand_idx.points):
        point_id = cand_idx.points[action.point]
        slots = cand_idx.point_to_slots.get(point_id, [])
```

**问题：**
- 如果`action.point >= len(cand_idx.points)`，这个action会被跳过
- 如果`point_id`不在`cand_idx.point_to_slots`中，`slots = []`
- 这些情况都不会报错，只是静默地跳过或返回空列表

**可能的原因：**
- cand_idx在某个时刻被更新或重新生成
- processed_sequence中的action的point索引是基于旧的cand_idx生成的
- 但生成slot_positions时使用的cand_idx是新的，导致point索引不匹配

#### 可能的问题点2：processed_sequence中的action不在原始sequence中

**虽然理论上不应该发生，但可能由于：**
- 中间件实现问题，创建了新的action（不是过滤，而是创建）
- 或者中间件修改了action的属性（point、atype等）
- 导致processed_sequence中的action在原始sequence中找不到对应的action

### 原因2：slot_positions从cand_idx获取，可能与候选快照不一致

#### 数据流追踪

**cand_idx的生成（第515行）：**
```python
candidates, cand_idx = self.enumerator.enumerate_with_index(...)
self._last_candidates[agent] = filtered_candidates
self._last_cand_idx[agent] = cand_idx
```

**在`enumerate_with_index`中（`logic/v5_enumeration.py`第235行）：**
```python
filtered_point_to_slots[point_id] = available_points[point_id]["slots"]

cand_idx = CandidateIndex(
    points=filtered_points,
    types_per_point=filtered_types,
    point_to_slots=filtered_point_to_slots,  # 从available_points获取
    ...
)
```

**所以`cand_idx.point_to_slots`是从`available_points[point_id]["slots"]`获取的。**

**候选快照的生成（第514行）：**
```python
for candidate in candidates:
    slots = candidate.meta.get("slots", [])
    # 检查槽位是否可用
    if slots_available:
        filtered_candidates.append(candidate)

self._last_candidates[agent] = filtered_candidates
```

**所以`_last_candidates`包含的是过滤后的候选（去除了已被占用的）。**

#### 不一致的可能来源

**问题1：cand_idx.point_to_slots和候选快照中的slots可能不同步**
- `cand_idx.point_to_slots`是在枚举候选时生成的，基于`available_points`
- `_last_candidates`是在枚举候选后过滤生成的，基于全局占用状态
- 如果某个point的所有候选都被过滤掉了（因为槽位被占用），但`cand_idx.point_to_slots`中还有这个point，就会不一致

**问题2：中间件过滤后，cand_idx可能未更新**
- 中间件过滤sequence时，不影响`_last_candidates`和`_last_cand_idx`
- 但如果中间件过滤了某些point（虽然中间件只过滤action，不直接过滤point），可能会导致不一致

**问题3：action.point索引和point_id不匹配**
- `action.point`是索引（0, 1, 2, ...），指向`cand_idx.points[index]`
- 如果`action.point`超出范围，或`cand_idx.points[action.point]`对应的`point_id`不在`cand_idx.point_to_slots`中
- 就会获取不到slots

**问题4：slot_positions生成时从cand_idx获取，但EXEC从候选快照获取**
- EXEC从候选快照（`_last_candidates`）中查找cand，然后从`cand.meta.get("slots", [])`获取
- slot_positions从`cand_idx.point_to_slots`获取
- 这两个来源可能不一致，如果候选快照中的cand有slots，但`cand_idx.point_to_slots`中没有对应的point_id

### 关键发现

**`_build_slot_positions_from_snapshot`有两种路径：**

1. **路径1（多动作模式）：从cand_idx获取（第876行）**
   ```python
   if agent in self._last_cand_idx and self._last_cand_idx[agent]:
       cand_idx = self._last_cand_idx[agent]
       for action in sequence.actions:  # sequence是原始sequence
           if hasattr(action, 'point') and action.point < len(cand_idx.points):
               point_id = cand_idx.points[action.point]
               slots = cand_idx.point_to_slots.get(point_id, [])  # 从cand_idx获取
   ```

2. **路径2（单动作模式）：从候选快照获取（第898行）**
   ```python
   else:
       legacy_ids = sequence.get_legacy_ids()
       for action_id in legacy_ids:
           cand = self._get_candidate_from_snapshot(agent, action_id)
           if not cand:
               continue
           for slot_id in cand.meta.get("slots", []):  # 从候选快照获取
   ```

**问题：**
- 路径1使用原始sequence，从cand_idx获取
- 路径2使用原始sequence，从候选快照获取
- **但EXEC执行的是processed_sequence，从候选快照获取**

**所以31个槽位的情况可能是：**
1. 它们在processed_sequence中（EXEC执行了）
2. 它们在候选快照中（EXEC能从候选快照中获取到）
3. 但它们可能不在原始sequence中（如果中间件创建了新action）
4. 或者它们在原始sequence中，但point索引或point_id不匹配（导致slot_positions获取不到）

## 深度剖析两个可能的原因

### 原因1：slot_positions使用了原始sequence，而不是processed_sequence

#### 问题场景

**执行流程：**
```python
# 第1532行：获取原始sequence
sequence = phase_sequences[agent]  # 包含AtomicAction对象

# 第1542行：生成processed_sequence（经过中间件过滤）
processed_sequence = self._apply_middleware(agent, sequence)
# 中间件过滤后，processed_sequence中的action应该都在原始sequence中

# 第1548行：EXEC执行processed_sequence
reward, reward_terms = self._execute_agent_sequence(agent, processed_sequence)

# 第1555行：SEQUENCE标记使用processed_sequence ✅
self._update_occupied_slots_for_agent(agent, processed_sequence)

# 第1595行：slot_positions使用原始sequence ❌
sequence = phase_sequences[agent]  # ❌ 这是原始sequence
slot_positions = self._build_slot_positions_from_snapshot(agent, sequence)
```

#### 中间件过滤机制

**从中间件代码看：**

1. **candidate_range和river_restriction：**
   - 处理的是`action_id`（int）
   - 返回的Sequence中的actions是int列表
   - 这些int会被转换为AtomicAction（在某个地方）

2. **unlock_gate：**
   - 处理的是`action`对象（AtomicAction）
   - 返回的Sequence中的actions是AtomicAction对象列表
   - 直接保留action对象，不修改

**所以processed_sequence中的action应该都在原始sequence中（如果unlock_gate是最后一个中间件）。**

#### 可能的问题点1：action.point索引超出范围

**在`_build_slot_positions_from_snapshot`中（第874行）：**
```python
for action in sequence.actions:  # sequence是原始sequence
    if hasattr(action, 'point') and action.point < len(cand_idx.points):
        point_id = cand_idx.points[action.point]
        slots = cand_idx.point_to_slots.get(point_id, [])
```

**如果`action.point >= len(cand_idx.points)`，这个action会被跳过，不会报错。**

**可能的原因：**
- cand_idx在某个时刻被更新或重新生成
- processed_sequence中的action的point索引是基于旧的cand_idx生成的
- 但生成slot_positions时使用的cand_idx是新的，导致point索引不匹配

#### 可能的问题点2：point_id不在cand_idx.point_to_slots中

**如果`point_id`不在`cand_idx.point_to_slots`中，`slots = []`，不会报错。**

**可能的原因：**
- cand_idx在生成时，某些point被过滤掉了（没有可用类型）
- 但processed_sequence中的action仍然指向这些point
- 导致从cand_idx.point_to_slots获取不到slots

### 原因2：slot_positions从cand_idx获取，可能与候选快照不一致

#### 数据流追踪

**cand_idx的生成：**
```python
# 在enumerate_with_index中（logic/v5_enumeration.py第235行）
filtered_point_to_slots[point_id] = available_points[point_id]["slots"]

cand_idx = CandidateIndex(
    points=filtered_points,
    types_per_point=filtered_types,
    point_to_slots=filtered_point_to_slots,  # 从available_points获取
    ...
)
```

**候选快照的生成：**
```python
# 在get_action_candidates_with_index中（city_env.py第514行）
for candidate in candidates:
    slots = candidate.meta.get("slots", [])
    if slots_available:
        filtered_candidates.append(candidate)

self._last_candidates[agent] = filtered_candidates
```

#### 不一致的可能来源

**问题1：cand_idx.point_to_slots和候选快照中的slots可能不同步**
- `cand_idx.point_to_slots`是在枚举候选时生成的，基于`available_points`
- `_last_candidates`是在枚举候选后过滤生成的，基于全局占用状态
- 如果某个point的所有候选都被过滤掉了（因为槽位被占用），但`cand_idx.point_to_slots`中还有这个point，就会不一致

**问题2：action.point索引和point_id不匹配**
- `action.point`是索引（0, 1, 2, ...），指向`cand_idx.points[index]`
- 如果`action.point`超出范围，或`cand_idx.points[action.point]`对应的`point_id`不在`cand_idx.point_to_slots`中
- 就会获取不到slots

**问题3：slot_positions生成时从cand_idx获取，但EXEC从候选快照获取**
- EXEC从候选快照（`_last_candidates`）中查找cand，然后从`cand.meta.get("slots", [])`获取
- slot_positions从`cand_idx.point_to_slots`获取
- 这两个来源可能不一致

#### 关键发现

**`_build_slot_positions_from_snapshot`有两种路径：**

1. **路径1（多动作模式）：从cand_idx获取（第876行）**
   ```python
   if agent in self._last_cand_idx and self._last_cand_idx[agent]:
       cand_idx = self._last_cand_idx[agent]
       for action in sequence.actions:  # sequence是原始sequence
           if hasattr(action, 'point') and action.point < len(cand_idx.points):
               point_id = cand_idx.points[action.point]
               slots = cand_idx.point_to_slots.get(point_id, [])  # 从cand_idx获取
   ```

2. **路径2（单动作模式）：从候选快照获取（第898行）**
   ```python
   else:
       legacy_ids = sequence.get_legacy_ids()
       for action_id in legacy_ids:
           cand = self._get_candidate_from_snapshot(agent, action_id)
           if not cand:
               continue
           for slot_id in cand.meta.get("slots", []):  # 从候选快照获取
   ```

**问题：**
- 路径1使用原始sequence，从cand_idx获取（多动作模式）
- 路径2使用原始sequence，从候选快照获取（单动作模式）
- **但EXEC执行的是processed_sequence，从候选快照获取**

**如果使用的是多动作模式（路径1），就会出现不一致：**
- EXEC从候选快照获取（可能有slots）
- slot_positions从cand_idx获取（可能没有slots）

## 总结

**核心问题：**
- 31个槽位被EXEC标记了（真正执行了）
- 但不在export文件中（slot_positions中没有）

**两个可能的原因：**

1. **slot_positions使用了原始sequence，而不是processed_sequence**
   - 虽然理论上processed_sequence应该是原始sequence的子集
   - 但如果action.point索引超出范围，或point_id不在cand_idx.point_to_slots中，就会获取不到
   - 或者processed_sequence中有新的action（不在原始sequence中）

2. **slot_positions从cand_idx获取，可能与候选快照不一致**
   - cand_idx.point_to_slots和候选快照中的cand.meta["slots"]可能不一致
   - 导致EXEC能从候选快照中获取到，但slot_positions从cand_idx中获取不到
   - 或者action.point索引和point_id不匹配

**需要进一步验证这两个原因的具体情况。**

详细验证方案见 `验证方案.md`。

## 补充：用户的困惑

**用户的困惑：**
- 31个槽位都不在export文件中
- 但只有4个没有SEQUENCE标记，27个有SEQUENCE标记
- 用户觉得如果这31个槽位都不在export文件中，它们应该有某种共同特征（比如都没有SEQUENCE标记）
- 但实际上SEQUENCE标记情况不一致

**可能的原因：**
- SEQUENCE标记从action.meta获取（processed_sequence中）
- slot_positions从cand_idx.point_to_slots获取（原始sequence中）
- 这两个来源不一致，导致即使SEQUENCE标记了，slot_positions也可能获取不到

**详细分析见 `1031-4-补充-矛盾分析.md`。**

