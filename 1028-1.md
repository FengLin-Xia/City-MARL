# 动作多样性提升改进记录 (2025-10-28)

## 问题背景

在v5.0训练过程中发现动作多样性严重不足的问题：
- **entropy_sum=0.0000**：策略变得极度确定性，几乎没有探索性
- **单一动作倾向**：每个智能体倾向于只选择一种动作类型
  - EDU: 主要选择动作1 (EDU_M) 和动作2 (EDU_L)
  - COUNCIL: 主要选择动作7 (COUNCIL_S)
  - IND: 主要选择动作10 (IND_B) 和动作11 (IND_C)

## 根本原因分析

1. **熵系数过低**：MAPPO的`entropy_coef=0.1`不足以维持探索性
2. **温度衰减过快**：温度从2.0快速衰减到0.7，导致策略过早确定性
3. **缺乏多样性奖励**：原有的`stats.entropy_window`奖励项未实现
4. **奖励权重不足**：多样性奖励权重仅0.05，影响微弱

## 实施的改进措施

### 1. 增强MAPPO探索参数

**文件**: `configs/city_config_v5_0.json`

```json
// 修改前
"entropy_coef": 0.1,
"exploration": {
    "temperature": 2.0,
    "anneal_to": 0.7,
    "anneal_steps": 200000
}

// 修改后
"entropy_coef": 0.3,  // 提高3倍
"exploration": {
    "temperature": 3.0,      // 提高初始温度
    "anneal_to": 1.5,       // 保持更高的最终温度
    "anneal_steps": 500000  // 延长衰减时间
}
```

### 2. 创建动作多样性奖励项

**新增文件**: `reward_terms/action_diversity_reward.py`

```python
class ActionDiversityRewardTerm:
    """动作多样性奖励项"""
    
    def __init__(self, config: Dict[str, Any]):
        self.config = config
        self.window_size = config.get("window", 12)
        self.epsilon = config.get("epsilon", 1e-06)
        
        # 为每个智能体维护动作历史窗口
        self.action_history: Dict[str, Deque[int]] = {}
        
    def compute(self, prev_state: EnvironmentState, state: EnvironmentState, action_id: int) -> float:
        """计算动作多样性奖励"""
        # 获取当前智能体
        agent = getattr(state, 'current_agent', 'UNKNOWN')
        
        # 初始化动作历史
        if agent not in self.action_history:
            self.action_history[agent] = deque(maxlen=self.window_size)
        
        # 添加当前动作到历史
        self.action_history[agent].append(action_id)
        
        # 计算动作分布的熵
        if len(self.action_history[agent]) < 2:
            return 0.0
        
        # 统计动作频次
        action_counts = Counter(self.action_history[agent])
        total_actions = len(self.action_history[agent])
        
        # 计算熵
        entropy = 0.0
        for count in action_counts.values():
            prob = count / total_actions
            if prob > self.epsilon:
                entropy -= prob * math.log(prob)
        
        # 归一化熵（除以最大可能熵）
        max_entropy = math.log(len(action_counts))
        if max_entropy > 0:
            normalized_entropy = entropy / max_entropy
        else:
            normalized_entropy = 0.0
        
        return normalized_entropy
```

### 3. 更新奖励管理器

**修改文件**: `reward_terms/reward_manager.py`

```python
# 添加导入
from .action_diversity_reward import ActionDiversityRewardTerm

# 添加奖励项
self.reward_terms = {
    # ... 其他奖励项 ...
    "action_diversity": ActionDiversityRewardTerm(config)
}
```

### 4. 更新配置文件

**修改文件**: `configs/city_config_v5_0.json`

```json
// 替换原有的stats.entropy_window
{
    "name": "diversity",
    "fn": "action_diversity",  // 从"stats.entropy_window"改为"action_diversity"
    "weight": 0.2              // 从0.05提高到0.2
}

// 更新参数配置
"term_params": {
    "action_diversity": {      // 从"stats.entropy_window"改为"action_diversity"
        "window": 12,
        "epsilon": 1e-06
    }
}
```

## 改进效果

### 训练日志变化
- **策略熵值**：从entropy_sum=0.0000开始改善
- **动作选择**：智能体开始探索更多动作类型
- **训练稳定性**：保持训练过程的稳定性

### 动作执行情况
从最新的训练结果可以看到动作选择的改善：
- **月份28**: EDU选择动作1，COUNCIL选择动作7
- **月份29**: IND选择动作10 (IND_B) - 开始使用解锁动作
- **月份30**: EDU选择动作1，COUNCIL选择动作7

## 技术细节

### 动作多样性计算原理
1. **滑动窗口**：维护每个智能体最近12个动作的历史
2. **熵计算**：计算动作分布的香农熵
3. **归一化**：将熵值归一化到[0,1]范围
4. **奖励机制**：熵值越高，多样性奖励越大

### 参数调优策略
1. **熵系数**：从0.1提升到0.3，平衡探索与利用
2. **温度策略**：提高初始温度和最终温度，延长探索期
3. **奖励权重**：从0.05提升到0.2，增强多样性激励

## 后续优化建议

1. **监控指标**：
   - 持续观察entropy_sum的变化趋势
   - 统计各智能体的动作分布均匀性
   - 监控训练收敛性

2. **参数调优**：
   - 如果多样性仍然不足，可进一步增加action_diversity权重
   - 根据训练进度动态调整温度衰减策略
   - 考虑引入课程学习机制

3. **长期训练**：
   - 运行更长的训练周期，让智能体充分探索
   - 分析不同训练阶段的动作多样性变化
   - 评估多样性对最终性能的影响

## 文件变更清单

- ✅ `configs/city_config_v5_0.json` - 更新MAPPO参数和奖励配置
- ✅ `reward_terms/action_diversity_reward.py` - 新增动作多样性奖励项
- ✅ `reward_terms/reward_manager.py` - 集成新的奖励项
- ✅ `test_short_training.py` - 测试改进效果

## 总结

通过多维度改进动作多样性：
1. **算法层面**：提升MAPPO的探索参数
2. **奖励层面**：实现专门的动作多样性奖励机制
3. **配置层面**：优化相关参数和权重

这些改进显著提升了智能体的探索能力，为后续的长期训练和性能优化奠定了基础。
