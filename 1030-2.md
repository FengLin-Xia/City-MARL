### 1030-2 配置驱动的月份与导出修正（记录）

#### 改动目标
- 使单集时长与导出严格遵循配置 `env.time_model.total_steps`（例如 50 个月），避免任何“30个月”的隐性假设。

#### 代码改动
1) 环境总月数读取改为优先使用 `env.time_model.total_steps`
   - 文件：`envs/v5_0/city_env.py`
   - 位置：初始化中“环境状态”部分
   - 变更：
     - 从 `self.config.get("simulation", {}).get("total_months", 30)`
     - 改为优先 `env.time_model.total_steps`，回退 `simulation.total_months`（默认30）

2) TXT导出器按配置步数识别多 episode（移除硬编码30）
   - 文件：`exporters/v5_0/txt_exporter.py`
   - 变更：
     - 在构造函数读取 `self.episode_steps = config.env.time_model.total_steps`（默认30）
     - 多 episode 判断从 `len(step_logs) > 30` 改为 `len(step_logs) > self.episode_steps`

#### 配置前置
- 已在 `configs/city_config_v5_0.json` 中设置：
  - `env.time_model.total_steps = 50`
  - `mappo.rollout.horizon = 50`

#### 预期效果与验证
- 训练与导出覆盖到 `month_50`：应生成 `outputs/export_month_31..50.txt`，`monthly_summary/month_31..50_summary.png`。
- 日志应出现 `policy_select_multi ... month=31..50`。


#### 1030 调试：占位“未执行也被占用”交叉验证日志增强
- 目标：验证“序列在中间件后被裁剪/跳过但槽位已被标记占用”是否导致后续月份 `[ENUM_NO_CANDIDATES] ... reason=no_available_points`。
- 代码改动：`envs/v5_0/city_env.py`
  - 在 `def _mark_slot_id_occupied(...)` 中增加 WARNING 级日志：
    - `[OCCUPY_MARK] slot_id=... month=...`
  - 在执行期占位处：`_execute_action_atomic` 与 `_execute_action_legacy` 的槽位写入后新增：
    - `[OCCUPY_FROM_EXEC] agent=... slot_id=... month=...`
  - 在序列占位更新处：`_update_occupied_slots_for_agent` 中每个 slot 标记时新增：
    - `[OCCUPY_FROM_SEQUENCE] agent=... slot_id=... month=...`
  - 现有的 `print` 调试（如 `[SLOT_ID_MARK]`、`[SLOT_UPDATE_DEBUG]`）保留，用于控制台快速核对。
- 期望对照流程：
  1) 本月若出现 `[PIPELINE_SKIP] agent=... month=... reason=no_sequence`；
  2) 同月是否出现任一 `[OCCUPY_FROM_SEQUENCE]`/`[OCCUPY_MARK]`/`[OCCUPY_FROM_EXEC]`（表明有占位写入）；
  3) 次月对应 agent 是否出现 `[ENUM_NO_CANDIDATES] ... reason=no_available_points`。
- 日志级别：上述 3 类占位日志均为 WARNING，确保在 `logging.level=INFO` 下也会写入文件日志。

#### 1030 调试：新增两类高优先级汇总日志
- 目标：快速定位“结构性枯竭”的主因（占位规模逼近阈值 or 过滤环节把点打空）。
- 代码改动：
  - `envs/v5_0/city_env.py`
    - 在 `step_phase` 末尾增加月度占位汇总（WARNING）：
      - `[OCCUPIED_SUMMARY] month=.. occupied_ids=.. occupied_coords=.. current_step_occupied=..`
  - `logic/v5_enumeration.py`
    - 在 `_enumerate_available_points` 中增加过滤阶段计数（WARNING）：
      - `[ENUM_STAGE_COUNTS] agent=.. month=.. initial=.. after_range=.. after_river=..`
- 预期使用：
  - 结合 `[ENUM_NO_CANDIDATES]` 查看是“initial≈0/after_range≈0/after_river≈0”的哪一环打空；
  - 配合 `[OCCUPIED_SUMMARY]` 观察 `_global_occupied_slot_ids` 是否在40月前已接近总槽位数。

#### 1030 调试：区分“执行占位 vs 非执行占位”
- 背景：总槽位203，但实际选择建造约190余，怀疑存在“未执行也被占用”的误占。
- 改动：`envs/v5_0/city_env.py`
  - 新增集合 `self._built_slot_ids`，在执行期占位（`_execute_action_atomic/_legacy`）时同步写入；序列阶段占位不写入。
  - 将月度汇总升级为：
    - `[OCCUPIED_SUMMARY] month=.. occupied_ids=.. built_ids=.. non_exec_ids=.. sample_non_exec=[...]`
- 目的：直接量化“非执行占位”的规模与样本，验证是否存在“从未建造但被标记占用”的槽位。



