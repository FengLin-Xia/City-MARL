# 持续奖励与协同效应系统设计

## 现有系统现状分析

### v5.0 当前奖励计算系统

**核心组件**：
- `V5RewardCalculator`：统一的奖励计算器，基于v4.1的CRP逻辑
- `envs/v5_0/city_env.py`：环境包装器，负责奖励计算和budget更新

**当前计算流程**：
```python
# 1. 动作执行时计算奖励
def _compute_reward(self, agent: str, candidate: ActionCandidate) -> Tuple[float, Dict[str, float]]:
    # 获取当前环境状态
    current_state = self._get_current_environment_state()
    
    # 使用V5RewardCalculator计算奖励
    reward_terms = self.reward_calculator.calculate_reward(candidate, current_state)
    
    # 计算总奖励（一次性）
    total_reward = reward_terms.revenue + reward_terms.cost + reward_terms.prestige + ...
    
    return total_reward, reward_dict
```

**V5RewardCalculator 计算内容**：
- **基础奖励**：`base_reward` + `zone_reward` - `opex` + `river_premium`
- **成本计算**：`base_cost` + `zone_cost` + `land_price_cost`
- **邻近奖励**：`proximity_bonus`（基于邻近建筑）
- **规模奖励**：`size_bonus`（基于建筑尺寸）
- **工业集群协同**：`cluster_bonus`（已实现但仅一次性）

**当前问题**：
1. **一次性奖励**：动作执行后只获得一次奖励，没有持续收益
2. **无月度更新**：`advance_month()` 只清空槽位占用，不更新budget
3. **无持续收入**：建筑建造后不产生月度收入
4. **协同效应局限**：工业集群奖励只在建造时生效，不持续影响

### v4.1 参考系统

**双轨制设计**：
- **RL训练奖励**：固定NPV机制，一次性评估建造价值
- **Budget现金流**：持续月度收入，影响实际budget

**v4.1 持续奖励机制**：
```python
# RL训练奖励（一次性）
def _calculate_reward(self, agent: str, action: Action) -> float:
    if build_cost > 0:
        expected_lifetime = 12  # 固定回报期
        monthly_reward = action.reward
        future_income = monthly_reward * expected_lifetime
        npv = future_income - build_cost  # NPV计算
        return npv + progress_reward + cooperation_bonus - budget_penalty

# Budget现金流（持续）
def _calculate_monthly_income(self, agent: str) -> float:
    total_income = sum([asset['monthly_income'] for asset in self.active_assets[agent]])
    return total_income

# 每月更新budget
def _advance_turn(self):
    for agent in agents:
        monthly_income = self._calculate_monthly_income(agent)
        self.budgets[agent] += monthly_income  # 持续收入
```

**v4.1 关键特点**：
- **分离设计**：RL奖励和budget更新使用不同逻辑
- **持续收入**：每个建筑每月产生 `monthly_income`
- **资产跟踪**：`active_assets` 记录所有在营建筑
- **月度更新**：每月开始时累加所有建筑的月度收入

## 设计目标

实现一个动态的、全局的奖励系统，包含两个核心机制：

1. **持续奖励**：动作执行后，每月持续获得奖励
2. **协同效应**：某些动作会全局提升其他动作的持续奖励

## 实现策略对比

### 方案A：v4.1双轨制（不推荐）
- **RL奖励**：固定NPV，一次性评估
- **Budget现金流**：持续月度收入
- **问题**：两套计算系统，容易不一致

### 方案B：v5.0统一系统（推荐）
- **统一计算**：RL奖励和budget更新使用同一套逻辑
- **状态驱动**：在EnvironmentState中维护所有奖励信息
- **动态更新**：每月重新计算所有持续奖励
- **协同效应**：全局影响所有相关建筑

## 关键设计决策

### 1. 统一计算系统
**选择**：使用v5.0的统一系统，避免v4.1的双轨制问题
**原因**：
- 保持RL奖励和budget更新的一致性
- 避免"reward leakage"问题
- 符合v5.0的架构设计原则

### 2. 状态驱动设计
**选择**：在EnvironmentState中维护所有奖励信息
**原因**：
- 状态集中管理，便于调试和监控
- 支持复杂的协同效应计算
- 与v5.0的契约系统一致

### 3. 每月重新计算
**选择**：每月开始时重新计算所有持续奖励
**原因**：
- 支持动态协同效应（如工业集群奖励的变化）
- 确保奖励计算的准确性
- 便于添加新的奖励机制

### 4. 全局协同效应
**选择**：协同效应影响所有相关建筑，不仅仅是新建筑
**原因**：
- 更符合现实世界的协同效应
- 鼓励长期规划和战略思考
- 增强游戏的策略深度

## 技术实现要点

### 1. 与现有系统的集成
**V5RewardCalculator扩展**：
- 保持现有的 `calculate_reward` 接口不变
- 添加 `_calculate_monthly_rewards` 方法计算持续奖励
- 在 `other` 字段中包含持续奖励信息

**EnvironmentState扩展**：
- 添加 `monthly_rewards`、`synergy_activations` 等字段
- 保持向后兼容性
- 支持状态序列化和导出

### 2. 性能优化考虑
**计算复杂度**：
- 每月重新计算所有建筑奖励：O(n)，n为建筑数量
- 协同效应检查：O(1)，基于预计算的激活状态
- 总体性能影响：可接受（建筑数量通常<1000）

**缓存策略**：
- 基础奖励率缓存：避免重复读取配置
- 协同状态缓存：只在触发动作时更新
- 月度奖励缓存：每月开始时计算一次

### 3. 调试和监控
**日志输出**：
```python
logger.info(f"[MONTHLY_REWARDS] {agent}: {monthly_reward:.2f}, synergy_active={synergy_status}")
logger.info(f"[SYNERGY_ACTIVATION] action_id={action_id}, cluster_bonus={cluster_bonus}")
```

**状态导出**：
- 月度奖励历史
- 协同效应激活状态
- 建筑奖励率变化

## 核心机制

### 持续奖励机制

**基础逻辑**：
- 动作执行后，建筑每月产生持续收益
- 收益 = 基础奖励率 × 协同系数

**示例**：
```python
# 月份1：IND执行动作3（传统工业）
# 结果：每月持续获得100奖励

# 月份5：IND执行动作9（高级工业）
# 结果：动作3的持续奖励从100提升为115（+15%协同奖励）

# 月份6：动作3的持续奖励 = 115
# 月份7：动作3的持续奖励 = 115
# ...
```

### 协同效应机制

**触发条件**：
- 动作9、10、11（高级工业）被建造后
- 激活"工业集群"协同效应

**影响范围**：
- 动作3、4、5（传统工业）的持续奖励提升15%
- 全局效应：影响所有已建造和未来建造的传统工业建筑

## 数据结构设计

### EnvironmentState 扩展

```python
@dataclass
class EnvironmentState:
    """环境状态数据契约"""
    # 现有字段...
    land_prices: np.ndarray
    buildings: List[Building]
    slots: List[Slot]
    building_registry: Optional[BuildingRegistry] = None
    
    # 新增字段
    # 每个动作的当前持续奖励率（基础值）
    action_reward_rates: Dict[int, float] = field(default_factory=dict)
    
    # 协同关系的激活状态
    synergy_activations: Dict[str, bool] = field(default_factory=dict)
    
    # 每个agent的月度总奖励
    monthly_rewards: Dict[str, float] = field(default_factory=dict)
    
    # 每个agent的月度总奖励历史（用于调试）
    monthly_rewards_history: Dict[str, List[float]] = field(default_factory=dict)
```

### BuildingRegistry 扩展

```python
@dataclass
class BuildingRegistry:
    """建筑注册表 - 跟踪所有已建造建筑"""
    buildings: Dict[str, BuildingInfo] = field(default_factory=dict)
    action_counts: Dict[int, int] = field(default_factory=dict)
    unlock_status: Dict[int, bool] = field(default_factory=dict)
    
    # 新增字段
    # 按agent分组的建筑列表
    buildings_by_agent: Dict[str, List[str]] = field(default_factory=dict)
    
    # 按动作类型分组的建筑列表
    buildings_by_action: Dict[int, List[str]] = field(default_factory=dict)
```

## 计算流程设计

### 1. 每月开始时的奖励计算

```python
def _update_monthly_rewards(self):
    """每月开始时更新所有agent的持续奖励"""
    for agent in self.agents:
        total_reward = 0.0
        
        # 获取该agent的所有建筑
        agent_buildings = self.building_registry.buildings_by_agent.get(agent, [])
        
        for building_id in agent_buildings:
            building = self.building_registry.buildings[building_id]
            action_id = building.action_id
            
            # 获取该动作的当前奖励率（包含协同效应）
            current_rate = self._get_current_reward_rate(action_id)
            
            # 累加到总奖励
            total_reward += current_rate
        
        # 更新状态
        self.state.monthly_rewards[agent] = total_reward
        
        # 记录历史（用于调试）
        if agent not in self.state.monthly_rewards_history:
            self.state.monthly_rewards_history[agent] = []
        self.state.monthly_rewards_history[agent].append(total_reward)
```

### 2. 获取当前奖励率

```python
def _get_current_reward_rate(self, action_id: int) -> float:
    """获取动作的当前奖励率（包含协同效应）"""
    # 基础奖励率
    base_rate = self.state.action_reward_rates.get(action_id, 0.0)
    
    # 检查协同效应
    synergy_bonus = 0.0
    
    # 工业集群协同效应
    if action_id in [3, 4, 5]:  # 传统工业动作
        if self.state.synergy_activations.get("industrial_cluster", False):
            synergy_bonus = base_rate * 0.15  # 15%协同奖励
    
    return base_rate + synergy_bonus
```

### 3. 协同效应激活

```python
def _activate_synergy(self, action_id: int):
    """激活协同效应"""
    if action_id in [9, 10, 11]:  # 高级工业动作
        self.state.synergy_activations["industrial_cluster"] = True
        self.logger.info(f"工业集群协同效应已激活，动作{action_id}被建造")
```

### 4. 建筑注册表更新

```python
def _update_building_registry(self, agent: str, action_id: int, candidate: ActionCandidate, reward: float):
    """更新建筑注册表"""
    # 现有逻辑...
    
    # 新增：按agent和动作类型分组
    building_id = f"{agent}_{action_id}_{self.current_month}"
    
    # 更新按agent分组
    if agent not in self.building_registry.buildings_by_agent:
        self.building_registry.buildings_by_agent[agent] = []
    self.building_registry.buildings_by_agent[agent].append(building_id)
    
    # 更新按动作类型分组
    if action_id not in self.building_registry.buildings_by_action:
        self.building_registry.buildings_by_action[action_id] = []
    self.building_registry.buildings_by_action[action_id].append(building_id)
    
    # 激活协同效应
    self._activate_synergy(action_id)
```

## 集成到现有系统

### 1. 环境初始化

```python
def __init__(self, config: Dict[str, Any]):
    # 现有初始化...
    
    # 初始化动作奖励率
    self._initialize_action_reward_rates()
    
    # 初始化协同状态
    self.state.synergy_activations = {
        "industrial_cluster": False
    }

def _initialize_action_reward_rates(self):
    """初始化动作奖励率"""
    action_params = self.config.get("action_params", {})
    
    for action_id_str, params in action_params.items():
        action_id = int(action_id_str)
        base_reward = params.get("base_reward", 100.0)
        self.state.action_reward_rates[action_id] = base_reward
```

### 2. 每月流程

```python
def _advance_month(self):
    """推进到下一个月"""
    # 现有逻辑...
    
    # 新增：更新月度奖励
    self._update_monthly_rewards()
    
    # 更新budget（包含持续奖励）
    self._update_budgets_with_monthly_rewards()

def _update_budgets_with_monthly_rewards(self):
    """更新budget，包含持续奖励"""
    for agent in self.agents:
        monthly_reward = self.state.monthly_rewards.get(agent, 0.0)
        self.budgets[agent] += monthly_reward
        
        self.logger.info(f"[BUDGET_FLOW] {agent}: 持续奖励 +{monthly_reward:.1f}")
```

### 3. 奖励计算集成

```python
def _compute_reward(self, agent: str, action: ActionCandidate) -> float:
    """计算奖励（包含持续奖励）"""
    # 现有动作奖励计算
    action_reward = self.reward_calculator.calculate_reward(action, self.state)
    
    # 添加月度持续奖励
    monthly_reward = self.state.monthly_rewards.get(agent, 0.0)
    
    total_reward = action_reward + monthly_reward
    
    self.logger.info(f"奖励计算: agent={agent}, action={action.id}, "
                    f"action_reward={action_reward:.2f}, "
                    f"monthly_reward={monthly_reward:.2f}, "
                    f"total={total_reward:.2f}")
    
    return total_reward
```

## 配置参数

### city_config_v5_0.json 扩展

```json
{
  "reward_mechanisms": {
    "continuous_rewards": {
      "enabled": true,
      "action_reward_rates": {
        "3": 100.0,
        "4": 150.0,
        "5": 200.0,
        "9": 120.0,
        "10": 180.0,
        "11": 250.0
      }
    },
    "industrial_cluster_bonus": {
      "enabled": true,
      "bonus_rate": 0.15,
      "affects_actions": [3, 4, 5],
      "trigger_actions": [9, 10, 11],
      "global_effect": true
    }
  }
}
```

## 调试和监控

### 1. 日志输出

```python
def _log_monthly_rewards(self):
    """记录月度奖励详情"""
    for agent in self.agents:
        monthly_reward = self.state.monthly_rewards.get(agent, 0.0)
        synergy_status = self.state.synergy_activations.get("industrial_cluster", False)
        
        self.logger.info(f"[MONTHLY_REWARDS] {agent}: {monthly_reward:.2f}, "
                        f"synergy_active={synergy_status}")
```

### 2. 状态导出

```python
def get_state_summary(self) -> Dict[str, Any]:
    """获取状态摘要"""
    return {
        "monthly_rewards": self.state.monthly_rewards,
        "synergy_activations": self.state.synergy_activations,
        "building_counts": self.building_registry.action_counts,
        "monthly_rewards_history": self.state.monthly_rewards_history
    }
```

## 实现步骤

1. **扩展数据结构**：更新 `EnvironmentState` 和 `BuildingRegistry`
2. **实现计算逻辑**：添加月度奖励计算和协同效应激活
3. **集成到环境**：在每月流程中调用更新方法
4. **更新奖励计算**：在 `_compute_reward` 中包含持续奖励
5. **添加配置**：在配置文件中定义奖励率和协同参数
6. **测试验证**：确保协同效应正确激活和计算

## 预期效果

1. **持续激励**：建筑每月产生收益，鼓励长期规划
2. **协同效应**：高级工业建筑提升传统工业建筑的价值
3. **全局影响**：协同效应影响所有相关建筑，不仅仅是新建筑
4. **训练稳定**：奖励和budget保持一致，避免训练偏差

## 注意事项

1. **性能考虑**：每月重新计算所有奖励，需要优化计算效率
2. **状态一致性**：确保所有相关状态同步更新
3. **调试支持**：提供详细的日志和状态导出功能
4. **扩展性**：设计支持未来添加新的协同效应类型
