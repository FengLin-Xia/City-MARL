# 持续奖励+协同效应系统开发计划

## 🎯 项目概述

基于 `1028-3.md`（系统设计）、`1028-4.md`（RL奖励机制）和 `10.28-日志工厂.md`（日志系统），实现v5.0的持续奖励与协同效应系统。

### 核心目标
1. **持续奖励机制**：建筑每月产生持续收益，避免模型"躺平吃低保"
2. **协同效应机制**：高级工业建筑全局提升传统工业建筑收益
3. **增量奖励**：只奖励收益变化，防止策略塌缩
4. **潜势塑形**：为长期投资提供即时信用分配

## 📋 开发计划

### 阶段1：数据结构扩展（预计1-2天）

#### 1.1 扩展EnvironmentState
**文件**：`contracts/contracts.py`
```python
@dataclass
class EnvironmentState:
    # 现有字段...
    
    # 新增字段
    monthly_rewards: Dict[str, float] = field(default_factory=dict)
    synergy_activations: Dict[str, bool] = field(default_factory=dict)
    monthly_totals_history: Dict[str, List[float]] = field(default_factory=dict)
    action_reward_rates: Dict[int, float] = field(default_factory=dict)
```

#### 1.2 扩展BuildingRegistry
**文件**：`contracts/contracts.py`
```python
@dataclass
class BuildingRegistry:
    # 现有字段...
    
    # 新增字段
    buildings_by_agent: Dict[str, List[str]] = field(default_factory=dict)
    buildings_by_action: Dict[int, List[str]] = field(default_factory=dict)
```

#### 1.3 更新导出接口
**文件**：`contracts/__init__.py`
```python
from .contracts import (
    # 现有导出...
    EnvironmentState,
    BuildingRegistry
)
```

**验收标准**：
- [ ] 数据结构定义完成
- [ ] 向后兼容性测试通过
- [ ] 类型检查无错误

### 阶段2：持续奖励计算核心（预计2-3天）

#### 2.1 创建ContinuousRewardSystem类
**文件**：`logic/continuous_reward_system.py`
```python
class ContinuousRewardSystem:
    def __init__(self, config: Dict[str, Any]):
        self.config = config
        self.gamma = config.get("continuous_rewards", {}).get("gamma", 0.99)
        self.monthly_totals = {}  # 历史月度总收益
        
    def calculate_monthly_total_rewards(self, state: EnvironmentState) -> Dict[str, float]:
        """计算所有agent的月度总收益（包含协同效应）"""
        
    def calculate_delta_reward(self, agent: str, monthly_total_t: float) -> float:
        """计算增量奖励"""
        
    def calculate_potential_shaping(self, agent: str, monthly_total_t: float) -> float:
        """计算潜势塑形奖励"""
        
    def calculate_synergy_multiplier(self, action_id: int, state: EnvironmentState) -> float:
        """计算协同效应倍数"""
```

#### 2.2 实现月度总收益计算
```python
def calculate_monthly_total_rewards(self, state: EnvironmentState) -> Dict[str, float]:
    """计算所有agent的月度总收益（包含协同效应）"""
    total_rewards = {}
    
    for agent in self.agents:
        agent_total = 0.0
        
        # 获取该agent的所有建筑
        agent_buildings = state.building_registry.buildings_by_agent.get(agent, [])
        
        for building_id in agent_buildings:
            building = state.building_registry.buildings[building_id]
            action_id = building.action_id
            
            # 计算基础奖励率
            base_rate = state.action_reward_rates.get(action_id, 0.0)
            
            # 应用协同效应
            synergy_multiplier = self.calculate_synergy_multiplier(action_id, state)
            
            # 最终月度收益
            monthly_income = base_rate * synergy_multiplier
            agent_total += monthly_income
        
        total_rewards[agent] = agent_total
    
    return total_rewards
```

#### 2.3 实现增量奖励和潜势塑形
```python
def calculate_delta_reward(self, agent: str, monthly_total_t: float) -> float:
    """计算增量奖励"""
    if agent not in self.monthly_totals:
        self.monthly_totals[agent] = 0.0
    
    monthly_total_prev = self.monthly_totals[agent]
    delta_reward = monthly_total_t - monthly_total_prev
    
    # 更新历史记录
    self.monthly_totals[agent] = monthly_total_t
    
    return delta_reward

def calculate_potential_shaping(self, agent: str, monthly_total_t: float) -> float:
    """计算潜势塑形奖励"""
    if agent not in self.monthly_totals:
        return 0.0
    
    monthly_total_prev = self.monthly_totals[agent]
    
    # 潜势函数
    phi_s = monthly_total_prev / (1 - self.gamma)
    phi_sp = monthly_total_t / (1 - self.gamma)
    
    # 塑形项
    shaping_reward = self.gamma * phi_sp - phi_s
    
    return shaping_reward
```

**验收标准**：
- [ ] ContinuousRewardSystem类实现完成
- [ ] 月度总收益计算正确
- [ ] 增量奖励计算正确
- [ ] 潜势塑形计算正确
- [ ] 单元测试通过

### 阶段3：协同效应机制（预计1-2天）

#### 3.1 实现协同效应计算
```python
def calculate_synergy_multiplier(self, action_id: int, state: EnvironmentState) -> float:
    """计算协同效应倍数"""
    multiplier = 1.0
    
    # 工业集群协同效应
    synergy_config = self.config.get("synergy_effects", {}).get("industrial_cluster", {})
    if synergy_config.get("enabled", False):
        affects_actions = synergy_config.get("affects_actions", [3, 4, 5])
        trigger_actions = synergy_config.get("trigger_actions", [9, 10, 11])
        
        if action_id in affects_actions:
            if self._is_synergy_active(state, trigger_actions):
                bonus_rate = synergy_config.get("bonus_rate", 0.15)
                multiplier += bonus_rate
    
    return multiplier

def _is_synergy_active(self, state: EnvironmentState, trigger_actions: List[int]) -> bool:
    """检查协同效应是否激活"""
    if not hasattr(state, 'building_registry') or not state.building_registry:
        return False
    
    # 检查是否有触发动作被建造
    for action_id in trigger_actions:
        if state.building_registry.action_counts.get(action_id, 0) > 0:
            return True
    
    return False
```

#### 3.2 协同效应激活逻辑
```python
def activate_synergy(self, action_id: int, state: EnvironmentState):
    """激活协同效应"""
    synergy_config = self.config.get("synergy_effects", {}).get("industrial_cluster", {})
    if synergy_config.get("enabled", False):
        trigger_actions = synergy_config.get("trigger_actions", [9, 10, 11])
        
        if action_id in trigger_actions:
            state.synergy_activations["industrial_cluster"] = True
            logger.info(f"工业集群协同效应已激活，动作{action_id}被建造")
```

**验收标准**：
- [ ] 协同效应计算正确
- [ ] 协同效应激活逻辑正确
- [ ] 工业集群奖励正确应用
- [ ] 单元测试通过

### 阶段4：环境集成（预计2-3天）

#### 4.1 环境初始化
**文件**：`envs/v5_0/city_env.py`
```python
def __init__(self, config_path: str):
    # 现有初始化...
    
    # 初始化持续奖励系统
    self.continuous_reward_system = ContinuousRewardSystem(self.config)
    
    # 初始化动作奖励率
    self._initialize_action_reward_rates()
    
    # 初始化协同状态
    self.state.synergy_activations = {
        "industrial_cluster": False
    }

def _initialize_action_reward_rates(self):
    """初始化动作奖励率"""
    action_params = self.config.get("action_params", {})
    
    for action_id_str, params in action_params.items():
        action_id = int(action_id_str)
        base_reward = params.get("base_reward", 100.0)
        self.state.action_reward_rates[action_id] = base_reward
```

#### 4.2 每月流程更新
```python
def advance_month(self):
    """推进到下一个月"""
    # 现有逻辑...
    
    # 新增：计算月度总收益
    monthly_totals = self.continuous_reward_system.calculate_monthly_total_rewards(self.state)
    
    # 更新budget（包含持续收益）
    for agent in self.agents:
        monthly_income = monthly_totals.get(agent, 0.0)
        self.budgets[agent] += monthly_income
        
        if topic_enabled("budget_flow"):
            self.logger.info(f"[BUDGET_FLOW] {agent}: 持续收益 +{monthly_income:.1f}")
```

#### 4.3 奖励计算更新
```python
def _compute_reward(self, agent: str, candidate: ActionCandidate) -> Tuple[float, Dict[str, float]]:
    """计算奖励 - 使用新的持续奖励系统"""
    # 现有动作奖励计算
    reward_terms = self.reward_calculator.calculate_reward(candidate, self.state)
    action_reward = reward_terms.revenue + reward_terms.cost + reward_terms.prestige + reward_terms.proximity + reward_terms.diversity
    
    # 计算月度总收益
    monthly_totals = self.continuous_reward_system.calculate_monthly_total_rewards(self.state)
    monthly_total_t = monthly_totals.get(agent, 0.0)
    
    # 计算增量奖励
    delta_reward = self.continuous_reward_system.calculate_delta_reward(agent, monthly_total_t)
    
    # 计算潜势塑形
    shaping_reward = self.continuous_reward_system.calculate_potential_shaping(agent, monthly_total_t)
    
    # 总训练奖励
    total_reward = action_reward + delta_reward + shaping_reward
    
    # 日志输出
    if topic_enabled("reward_terms"):
        self.logger.info(f"奖励计算: agent={agent}, action={candidate.id}, "
                        f"action_reward={action_reward:.2f}, "
                        f"delta_reward={delta_reward:.2f}, "
                        f"shaping_reward={shaping_reward:.2f}, "
                        f"total={total_reward:.2f}")
    
    return total_reward, reward_dict
```

#### 4.4 建筑注册表更新
```python
def _update_building_registry(self, agent: str, action_id: int, candidate: ActionCandidate, reward: float):
    """更新建筑注册表"""
    # 现有逻辑...
    
    # 新增：按agent和动作类型分组
    building_id = f"{agent}_{action_id}_{self.current_month}_{len(self.building_registry.buildings)}"
    
    # 更新按agent分组
    if agent not in self.building_registry.buildings_by_agent:
        self.building_registry.buildings_by_agent[agent] = []
    self.building_registry.buildings_by_agent[agent].append(building_id)
    
    # 更新按动作类型分组
    if action_id not in self.building_registry.buildings_by_action:
        self.building_registry.buildings_by_action[action_id] = []
    self.building_registry.buildings_by_action[action_id].append(building_id)
    
    # 激活协同效应
    self.continuous_reward_system.activate_synergy(action_id, self.state)
```

**验收标准**：
- [ ] 环境初始化正确
- [ ] 每月流程更新正确
- [ ] 奖励计算集成正确
- [ ] 建筑注册表更新正确
- [ ] 集成测试通过

### 阶段5：配置和日志（预计1天）

#### 5.1 配置文件更新
**文件**：`configs/city_config_v5_0.json`
```json
{
  "continuous_rewards": {
    "enabled": true,
    "gamma": 0.99,
    "action_reward_rates": {
      "3": 100.0,
      "4": 150.0,
      "5": 200.0,
      "9": 120.0,
      "10": 180.0,
      "11": 250.0
    }
  },
  "synergy_effects": {
    "industrial_cluster": {
      "enabled": true,
      "bonus_rate": 0.15,
      "affects_actions": [3, 4, 5],
      "trigger_actions": [9, 10, 11],
      "global_effect": true
    }
  },
  "logging": {
    "topics": {
      "continuous_rewards": true,
      "synergy_activation": true,
      "monthly_rewards": true
    }
  }
}
```

#### 5.2 日志系统集成
**文件**：`utils/logger_factory.py`
```python
# 添加新的日志主题
"topics": {
    # 现有主题...
    "continuous_rewards": true,
    "synergy_activation": true,
    "monthly_rewards": true
}
```

**验收标准**：
- [ ] 配置文件更新完成
- [ ] 日志主题添加完成
- [ ] 日志输出正确

### 阶段6：测试和验证（预计2-3天）

#### 6.1 单元测试
**文件**：`test_continuous_reward_system.py`
```python
def test_monthly_total_rewards():
    """测试月度总收益计算"""
    
def test_delta_reward():
    """测试增量奖励计算"""
    
def test_potential_shaping():
    """测试潜势塑形计算"""
    
def test_synergy_multiplier():
    """测试协同效应计算"""
    
def test_synergy_activation():
    """测试协同效应激活"""
```

#### 6.2 集成测试
**文件**：`test_continuous_reward_integration.py`
```python
def test_environment_integration():
    """测试环境集成"""
    
def test_reward_calculation():
    """测试奖励计算集成"""
    
def test_budget_update():
    """测试budget更新"""
```

#### 6.3 训练测试
**文件**：`test_continuous_reward_training.py`
```python
def test_short_training():
    """测试短期训练"""
    
def test_synergy_effect():
    """测试协同效应"""
    
def test_reward_consistency():
    """测试奖励一致性"""
```

**验收标准**：
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 训练测试通过
- [ ] 性能测试通过

## 🔧 技术实现细节

### 日志系统集成
基于 `10.28-日志工厂.md`，使用配置驱动的日志系统：

```python
# 使用主题开关控制日志输出
if topic_enabled("continuous_rewards"):
    logger.info(f"[CONTINUOUS_REWARDS] {agent}: {monthly_reward:.2f}")

if topic_enabled("synergy_activation"):
    logger.info(f"[SYNERGY_ACTIVATION] action_id={action_id}, cluster_bonus={cluster_bonus}")

if topic_enabled("monthly_rewards"):
    logger.info(f"[MONTHLY_REWARDS] {agent}: {monthly_reward:.2f}, synergy_active={synergy_status}")
```

### 性能优化
1. **缓存策略**：基础奖励率缓存，协同状态缓存
2. **计算优化**：每月只计算一次月度总收益
3. **内存管理**：合理管理历史数据

### 错误处理
1. **配置验证**：检查配置文件完整性
2. **状态一致性**：确保所有状态同步更新
3. **异常处理**：优雅处理计算错误

## 📊 验收标准

### 功能验收
- [ ] 持续奖励机制正常工作
- [ ] 协同效应正确激活和计算
- [ ] 增量奖励防止策略塌缩
- [ ] 潜势塑形提供即时信用
- [ ] RL奖励和budget更新一致

### 性能验收
- [ ] 计算性能可接受（<100ms/月）
- [ ] 内存使用合理（<100MB增长）
- [ ] 日志输出不影响性能

### 质量验收
- [ ] 代码覆盖率>90%
- [ ] 类型检查无错误
- [ ] 文档完整
- [ ] 向后兼容性

## 🚀 部署计划

### 开发环境
1. **分支策略**：创建 `feature/continuous-rewards` 分支
2. **代码审查**：每个阶段完成后进行代码审查
3. **持续集成**：自动运行测试和类型检查

### 测试环境
1. **功能测试**：完整功能测试
2. **性能测试**：性能基准测试
3. **兼容性测试**：向后兼容性测试

### 生产环境
1. **灰度发布**：先在部分环境测试
2. **监控部署**：监控系统性能
3. **回滚准备**：准备回滚方案

## 📝 风险评估

### 技术风险
- **性能影响**：每月重新计算可能影响性能
- **状态一致性**：多状态同步更新可能出错
- **内存泄漏**：历史数据积累可能导致内存问题

### 缓解措施
- **性能优化**：使用缓存和优化算法
- **状态管理**：使用事务性更新
- **内存管理**：定期清理历史数据

### 业务风险
- **训练不稳定**：新的奖励机制可能影响训练
- **策略变化**：模型行为可能发生重大变化

### 缓解措施
- **渐进式部署**：逐步启用新功能
- **A/B测试**：对比新旧系统效果
- **监控告警**：实时监控训练状态

## 📈 成功指标

### 技术指标
- 系统性能提升<10%
- 内存使用增长<20%
- 代码覆盖率>90%

### 业务指标
- 模型探索性提升（熵值增加）
- 长期规划能力增强
- 协同效应正确激活

### 用户体验
- 训练稳定性提升
- 奖励信号更清晰
- 调试信息更丰富

## 🎯 总结

这个开发计划基于三个核心文档，实现了：

1. **持续奖励机制**：建筑每月产生收益，鼓励长期规划
2. **协同效应机制**：高级工业建筑提升传统工业建筑价值
3. **增量奖励**：防止模型"躺平吃低保"
4. **潜势塑形**：为长期投资提供即时信用
5. **日志系统集成**：提供丰富的调试信息

通过分阶段实施，确保系统稳定性和可维护性，最终实现一个既符合RL训练需求又具有现实意义的奖励系统。

