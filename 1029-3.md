# 2025-10-29 多样性诊断日志与配置改动记录

## 目的
为定位“各智能体动作趋于单一（探索不足/确定性过强）”的问题，添加最小粒度的可开关日志，用于：
- 统计候选阶段各动作类型的可用性（是否存在结构性稀缺）
- 观察多动作两阶段采样中的类型分布（策略层是否压制特定类型）

## 代码改动
- 文件：`logic/v5_enumeration.py`
  - 位置：`enumerate_with_index`（构建 `CandidateIndex` 之后）
  - 新增日志（原受 `logging.topics.candidates` 控制，现已强制输出）：
    - `[CANDIDATE_STATS] agent=... month=... available_by_type={action_id: count}`
  - 2025-10-29（增量）：将该日志级别从 `info` 提升为 `warning`，并移除 `topic_enabled/sampling_allows` gating，保证任何配置下均可见。

- 文件：`solvers/v5_0/rl_selector.py`
  - 位置：`select_action_multi`
  - 新增日志（全部强制输出，WARNING 级别）：
    - `[TYPE_BANNER]`：入口横幅，显示进入多动作采样时的关键统计（points/types_total）
    - `[TYPE_CHECK]`：提前返回检查前的条件确认（multi_enabled/cand_idx/points_len）
    - `[TYPE_FALLBACK]`：如果降级到单动作，输出降级标记
    - `[TYPE_NET_INIT]` / `[TYPE_NET_READY]`：网络初始化前后确认
    - `[TYPE_LOOP_START]`：进入循环前的确认（max_k）
    - `[TYPE_ITER]`：每轮迭代开始的确认（step=k）
    - `[TYPE_POINT_DISTR]`：选点前输出点分布摘要（valid_points、top_point_prob、stop_prob）
    - `[TYPE_COUNTS_BEFORE]`：选点前、按类型汇总的可用数量（考虑点与类型掩蔽）
    - `[TYPE_BEFORE_STOP]`：做出是否 STOP 的选择前，输出 choice_idx 与 stop_index
    - `[TYPE_EARLY_STOP]`：如果本步直接选择 STOP（未选类型），输出早停标记
    - `[TYPE_PROBS_POINT]`：在选定点上的类型概率分布（映射到动作ID）
    - `[TYPE_COUNTS_AFTER]`：掩蔽更新后再次统计的类型可用数量
  - 2025-10-29（增量）：修正类型掩蔽布尔判断，使用 `float(tmask[j].item()) > 0`。
  - 2025-10-29（增量）：所有 TYPE_* 日志级别从 `info` 提升为 `warning`，并移除 `topic_enabled/sampling_allows` gating，确保无条件输出；同时去除上述关键日志周围的 try/except，使异常不会被静默吞掉。
  - 2025-10-29（增量2）：添加 `[TYPE_CHECK]`、`[TYPE_FALLBACK]`、`[TYPE_NET_INIT]`、`[TYPE_NET_READY]` 等检查点日志，用于定位函数在循环开始前的提前返回问题。所有检查点日志均为强制 WARNING 级别输出。
  - 2025-10-29（Bug修复）：添加缺失的 `_encode_state` 方法。该方法从 `EnvironmentState` 对象中提取特征（月份、建筑数量、槽位数量、预算、地价、建筑统计、月度奖励、协同激活等），编码为固定大小（obs_size=64）的 torch.Tensor，用于神经网络输入。

## 配置改动
- 文件：`configs/city_config_v5_0.json`
  - `logging.modules.enumeration`: 从 `"WARN"` 调整为 `"INFO"`
  - 新增 `logging.modules.policy: "INFO"`
  - 说明：确保普通信息级日志可写入 `logs/v5_0.log`；但为提高可见性，关键诊断日志已提升为 `warning` 且无条件输出。

## 输出位置与查看方式
- 日志文件：`logs/v5_0.log`
- 快速检索命令（PowerShell）：
  ```powershell
  Select-String -Path logs\v5_0.log -SimpleMatch -Pattern '[CANDIDATE_STATS]','[TYPE_BANNER]','[TYPE_CHECK]','[TYPE_FALLBACK]','[TYPE_NET_INIT]','[TYPE_NET_READY]','[TYPE_LOOP_START]','[TYPE_ITER]','[TYPE_POINT_DISTR]','[TYPE_COUNTS_BEFORE]','[TYPE_BEFORE_STOP]','[TYPE_EARLY_STOP]','[TYPE_PROBS_POINT]','[TYPE_COUNTS_AFTER]'
  ```

## 观察样例（本次运行）
- 示例（候选统计）：
  - `2025-10-29 21:47:13,662 [WARNING] enumeration: [CANDIDATE_STATS] agent=IND month=0 available_by_type={3: 4, 4: 4, 5: 4}`
  - 解读：month=0 时 3/4/5 的候选数量相当，月初并不存在类型可用性“结构性稀缺”。
- 示例（入口横幅）：
  - `2025-10-29 21:55:24,465 [WARNING] policy: [TYPE_BANNER] enter select_action_multi agent=IND month=0 points=4 types_total=12`
  - 解读：确认进入多动作采样路径，有效候选点数为 4，类型总数为 12。

## 预期结论方向（执行后验证）
- 若 `[CANDIDATE_STATS]` 显示大动作（如 4/5 或其他智能体的大型类型）计数显著偏低：为结构性稀缺，应优先检查候选生成与约束（等级、河流、范围、预算等）。
- 若可用计数不低，但 `[TYPE_PROBS_POINT]` 始终偏向小动作：为策略层偏置/奖励尺度问题，应审视温度、熵、stop_bias、reward scaling 与成本/距离惩罚的相对量级。
- 若 `[TYPE_EARLY_STOP]` 频繁出现：说明策略倾向于早停，可能是 stop_bias 过高或点分布过于集中。

## 追加：EDU 类型对比 What-if 与预算上下文日志（2025-10-29）

- 文件：`solvers/v5_0/rl_selector.py`
  - 位置：`select_action_multi`，在 `[TYPE_PROBS_POINT]` 之后（仅当 `agent==EDU`）
  - 新增诊断（WARNING 级，强制输出）：
    - `[TYPE_WHATIF_BASE]`：同一点上对可选类型（0/1/2）做基础项估算（不含空间项），并按 `rules.terms` 的权重给出加权总估计。
      - 输出字段：`revenue`（reward+rent）、`cost`、`opex`、`prestige`、`weighted_total_est`，以及各 term 的 `weights`
    - `[LEDGER_WHATIF]`：输出当前 `EDU` 与 `COUNCIL` 的预算、以及该点上各类型的 `cost`，用于观察共享预算带来的保守偏好。
  - 说明：
    - 该估算不会改动策略与执行，仅用于定位“在无 size bonus 下为何偏向动作0”的成因；
    - 未包含空间相关项（地价、邻近、河流等），用于先排查基础项与缩放/裁剪/权重带来的偏置。

- 快速检索命令（PowerShell）：
  ```powershell
  Select-String -Path logs\v5_0.log -SimpleMatch -Pattern '[TYPE_WHATIF_BASE]','[LEDGER_WHATIF]'
  ```
