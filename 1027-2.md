# 1027-2: 训练奖励计算系统不一致问题分析

## 问题发现

在分析训练过程中发现存在**两个不同的奖励计算系统**，导致训练信号与预算更新不一致。

## 两个计算系统对比

### 1. V5RewardCalculator（复杂系统）
- **用途**：预算更新和 `[BUDGET_FLOW]` 日志
- **调用位置**：`_execute_action_atomic` 和 `_execute_action_legacy`
- **特点**：
  - 包含4个子计算器：地价、邻近性、河流、规模
  - 复杂的成本计算：`base_cost + zone_cost + land_price_cost`
  - 复杂的收益计算：包含租金逻辑、地价放大因子、河流溢价
  - 支持区位常数（near/mid/far）
  - 支持污染惩罚
  - **返回负值成本**：`cost=-cost`

### 2. V5ActionScorer（简化系统）
- **用途**：训练奖励计算和 `奖励计算` 日志
- **调用位置**：`_compute_reward`
- **特点**：
  - 简单的线性计算
  - 基础奖励：`base_reward * lp_multiplier`
  - 基础成本：`base_cost * lp_multiplier`
  - 简化的邻近性和多样性奖励
  - **返回正值成本**：`cost=cost`

## 具体数值对比（动作0，地价=1.0）

### V5RewardCalculator
```
base_cost = 700, base_reward = 140
land_price_cost = 1.0 * 1000 = 1000
cost = 700 + 0 + 1000 = 1700
reward_base = 140 + 0 - 70 + 25 = 95
reward_lp_factor = 1.0 + 0.1 * 1.0 = 1.1
reward = 95 * 1.1 = 104.5
返回：cost=-1700, revenue=104.5
```

### V5ActionScorer
```
base_cost = 700, base_reward = 140
lp_multiplier = 1.0 + 1.0 * 0.3 = 1.3
cost = 700 * 1.3 = 910
lp_multiplier = 1.0 + 1.0 * 0.5 = 1.5
revenue = 140 * 1.5 = 210
返回：cost=910, revenue=210
```

## 问题影响

### 1. 训练信号不一致
- **预算更新**：使用 V5RewardCalculator 的复杂计算
- **模型学习**：使用 V5ActionScorer 的简化计算
- **结果**：模型学习到的奖励信号与实际预算变化不匹配

### 2. 环境复杂性缺失
- **V5ActionScorer** 只使用简单的地价乘数（0.5和0.3）
- **缺少**：河流溢价、复杂地价场、邻近性奖励、污染惩罚等
- **结果**：模型无法学习到复杂环境条件的影响

### 3. 训练信号分析
虽然训练信号包含"动作+选点"信息：
- `lp_norm`：来自具体槽位的地价
- `zone`：区位信息（near/mid/far）
- `slots`：具体选择的槽位ID

但**复杂环境条件的影响基本没有落到模型中**，因为：
- 地价计算过于简化
- 缺少环境复杂性机制
- 计算路径不一致

## 解决方案

### 方案1：统一使用V5RewardCalculator
- 修改 `_compute_reward` 使用 `self.reward_calculator`
- 确保训练和预算使用相同的计算逻辑

### 方案2：让V5ActionScorer调用V5RewardCalculator
- 在 `score_action` 中调用 `self.reward_calculator.calculate_reward`
- 保持接口一致性

### 方案3：重构奖励计算架构
- 设计统一的奖励计算接口
- 确保所有奖励计算路径使用相同的逻辑

## 解决方案实施

**✅ 已实施方案1**：直接修改 `_compute_reward` 使用 V5RewardCalculator

### 修改内容

1. **统一奖励计算路径**：
   - 修改 `envs/v5_0/city_env.py` 的 `_compute_reward` 方法
   - 移除对 `V5ActionScorer` 的依赖
   - 统一使用 `V5RewardCalculator` 计算奖励

2. **代码修改**：
   ```python
   # 修改前：使用V5ActionScorer
   reward_terms = self.scorer.score_action(candidate, current_state)
   total_reward = reward_terms.revenue - reward_terms.cost + ...
   
   # 修改后：使用V5RewardCalculator
   reward_terms = self.reward_calculator.calculate_reward(candidate, current_state)
   total_reward = reward_terms.revenue + reward_terms.cost + ...  # cost已经是负值
   ```

3. **移除依赖**：
   - 移除 `from logic.v5_scorer import V5ActionScorer`
   - 移除 `self.scorer = V5ActionScorer(self.config)`

### 预期效果

1. **训练信号一致性**：训练和预算使用相同的计算逻辑
2. **环境复杂性**：模型能学习到河流溢价、复杂地价场等
3. **数值稳定性**：更完整的数值处理和边界检查

### 风险评估

- **低风险**：V5RewardCalculator已经在预算系统中验证稳定
- **需要监控**：奖励尺度变化，可能需要调整学习率
- **建议**：渐进式实施，密切监控训练指标

## 相关文件

- `envs/v5_0/city_env.py`：`_compute_reward` 方法（已修改）
- `logic/v5_reward_calculator.py`：复杂奖励计算系统（统一使用）
- `logic/v5_scorer.py`：简化奖励计算系统（已弃用）
- `trainers/v5_0/ppo_trainer.py`：训练经验收集（已增强指标记录）
- `test_reward_unification.py`：测试脚本

## 时间记录

- 发现问题：2025-10-27
- 分析完成：2025-10-27
- 方案实施：2025-10-27
- 测试验证：待进行

