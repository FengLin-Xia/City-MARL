# v5.0 持续奖励系统风险缓解补充方案

## ⚙️ 概述
本补充方案旨在修正《1028-5.md 开发计划》中发现的三类潜在风险：
1. 性能假设过于乐观；
2. 奖励与预算状态一致性未完全闭环；
3. 调试与监控粒度不足。

---

## ⚠️ 一、性能优化补充

### 问题
当前设计中，系统每月 O(n) 重算所有建筑及协同收益，当建筑规模或协同数量增加时会成为主要瓶颈。

### 改进方案
1. **增量化计算（Dirty Flag）**
   - 每个收益体（建筑/项目）增加 `dirty` 标志与 `last_rate`。
   - 仅在有动作影响或协同参数变更时标记为 dirty。  
   - 月度更新时仅重算 dirty 实体。

2. **聚合缓存（Vectorized Aggregation）**
   - 按动作类型与 cluster 维度聚合统计：`count[type, cluster]`、`base_rate[type, cluster]`。
   - 先批量计算基础收益，个体差异仅在脏对象集合中更新。

3. **协同依赖版本号**
   - 为协同参数维护版本号；仅当版本变动时刷新缓存。

4. **性能基准测试**
   - 建筑数 500 / 2000 / 5000 三档测试，记录每月计算耗时。  
   - 目标指标：1000 样本下 <100ms，复杂度近似线性。

### 监控指标
| 指标 | 目标 |
|------|------|
| 计算耗时(ms) | <100@1000建筑 |
| 脏对象占比 | <10% |
| 缓存命中率 | >85% |

---

## ⚖️ 二、状态一致性强化

### 问题
目前 `ContinuousRewardSystem` 与 `V5RewardCalculator` 均能计算奖励，存在口径漂移风险。

### 改进方案
1. **唯一真理源（SSOT）**
   - 仅保留 `V5RewardCalculator` 作为主计算器。
   - 环境、预算、训练均通过统一接口获取奖励。

2. **口径区分**
   ```python
   def compute_rewards(state, mode="train"|"eval"):
       if mode == "train":
           return ΔReward + shaping + action_immediate
       else:
           return monthly_total  # 真实账面收益
   ```

3. **一致性断言**
   ```python
   assert abs(budget.monthly_total - reward_calc.monthly_total) < ε
   ```

4. **事务化更新顺序**
   - Step1：计算奖励 → Step2：预算入账 → Step3：RL 返回 r_train。

### 自动测试
- 单元测试验证 budget 与 reward_calc 的差值不超过 ε。
- 集成测试重放 episode，确认训练轨迹与预算轨迹一致。

---

## 🧭 三、日志与监控补充

### 设计目标
提供可追踪奖励路径与性能可视化，支持问题定位。

### 必备日志字段
| 模块 | 字段 | 说明 |
|------|------|------|
| 经济层 | `monthly_total`, `delta_monthly`, `sum_by_type`, `synergy_version` | 复盘 reward 口径 |
| RL 层 | `r_train`分解（ΔReward/action/Φ项/time_cost） | 分析奖励来源 |
| 性能层 | `calc_time_ms`, `dirty_count`, `cache_hit_ratio` | 观察增量优化效果 |
| 策略层 | `entropy`, `policy_KL`, `clip_fraction`, `value_loss` | 追踪策略塌缩或发散 |

### 报警规则
| 条件 | 告警 |
|------|------|
| `entropy < 0.1` 连续N次 | 策略塌缩 |
| `|budget - calc| > ε` | 奖励不一致 |
| 计算耗时>阈值且脏对象占比<5% | 缓存失效 |

### Dashboard 建议
- **策略健康度面板**：动作熵、直方图、KL、E[R|a,cluster]。
- **经济走势面板**：月度收益曲线、ΔReward、协同加成、维护费占比。
- **性能面板**：重算耗时、脏对象比例、缓存命中率。

---

## ✅ 四、实施清单

- [ ] 实现 Dirty-Flag + 协同版本号增量计算。  
- [ ] 统一奖励接口 compute_rewards(mode)。  
- [ ] 增加预算与奖励一致性断言。  
- [ ] 构建性能与一致性测试用例。  
- [ ] 增设日志字段与告警规则。  
- [ ] 建立三面板监控 Dashboard。

---

## ✅ 五、结论

开发计划总体可行；补充本方案后，系统将在：
- 性能上具备线性扩展能力；
- 状态上实现单一真理源，杜绝奖励漂移；
- 调试上可通过日志与仪表板迅速定位问题。

这三点构成 v5.0 持续奖励系统的稳定性核心保障。
