# 解锁机制调试记录 (2025-10-29)

## 问题描述

在训练过程中，IND智能体在15个月后仍然只选择动作3，没有使用解锁后的动作9、10、11。

## 问题排查

### 1. 解锁配置检查

**配置状态**：
- `action_unlocks.rules[0].after_month: 15`
- `官unlock_action_ids: [9, 10, 11]`
- IND的`action_ids`包含`[3, 4, 5, 9, 10, 11]`

### 2. 代码逻辑分析

**解锁流程**：
1. `_get_unlocked_actions()` 方法读取配置并计算解锁状态
2. 在月份 >= `after_month` 时，解锁动作9、10、11
3. 返回 `basic_actions | unlocked_actions` = `{3,4,5} ∪ {9,10,11}` = `{3,4,5,9,10,11}`
4. 传递给枚举器进行候选过滤

**理论分析**：
- 代码逻辑上应该是正确的
- 在月份15时，`_get_unlocked_actions()` 应该返回包含9,10,11的集合
- 枚举器应该正确过滤并保留这些动作

### 3. 问题诊断结果

通过代码检查和模拟测试，确认：
- ✅ **代码逻辑正常**：解锁机制在代码层面应该能正常工作
- ❌ **模型行为异常**：解锁后的动作9,10,11没有出现在训练结果中

**结论**：这不是代码问题，而是**模型训练过拟合/过早收敛**的问题。

- 模型在训练早期学会了只选择动作3
- 即使动作9,10,11在15个月后解锁并可用，模型已经固定在了动作3的策略上
- 模型没有充分探索新解锁的动作

## 调试修改

为了验证解锁机制是否真的生效，添加了**调试模式**：

### 修改文件
`envs/v5_0/city_env.py` 的 `_get_unlocked_actions()` 方法

### 修改内容

**原逻辑**（合并模式）：
```python
if has_agent_rules:
    final_actions = basic_actions | unlocked_actions  # 合并：{3,4,5,9,10,11}
    return final_actions
```

**调试模式**（替换模式）：
```python
if has_agent_rules:
    if unlocked_actions:
        # 解锁后，只返回解锁的动作（替换而不是合并）
        return unlocked_actions  # {9, 10, 11}
    else:
        # 未解锁时，返回基础动作
        return basic_actions  # {3, 4, 5}
```

### 调试目的

通过"替换模式"可以明确验证：
- **解锁前**：IND只能使用动作3,4,5
- **解锁后**：IND只能使用动作9,10,11

如果解锁机制正常：
- 解锁后IND应该切换到动作9,10,11
- 如果仍然出现动作3，说明解锁机制未生效

如果解锁机制正常但模型不切换：
- 说明是模型训练问题（过拟合/收敛）

## 下一步行动

1. **验证解锁机制**：运行训练，观察解锁后IND是否切换到动作9,10,11
2. **如果解锁机制正常**：需要研究模型训练策略，防止过早收敛
   - 增加探索率/熵奖励
   - 调整学习率
   - 使用curriculum learning
   - 在新动作解锁时重置部分策略参数
3. **恢复代码**：测试完成后，根据需求决定是否保持"替换模式"或恢复"合并模式"

## 月度预算计算修复

### 问题
在月度汇总PNG导出中，预算计算存在错误：遍历了该月所有step_logs并累加每个log的budget_snapshot，导致预算值被重复累加，显示异常。

### 修复
**文件**：`exporters/v5_0/monthly_summary_png.py`

**修复前（错误）**：
```python
# 在循环中重复累加每个step_log的budget
for log, state in zip(step_logs, env_states):
    if log.budget_snapshot:
        for agent_budget in log.budget_snapshot.values():
            total_budget += float(agent_budget)  # 重复累加！
```

**修复后（正确）**：
```python
# cost和reward仍然累加（这是一个月的所有动作的总和）
for log, state in zip(step_logs, env_states):
    # ... cost和reward累加逻辑 ...

# 修复：预算只取最后一个StepLog的budget_snapshot总和（避免重复累加）
if step_logs:
    last_log = step_logs[-1]
    if last_log.budget_snapshot:
        if isinstance(last_log.budget_snapshot, dict):
            total_budget = sum(
                sum(agent_budget.values()) if isinstance(agent_budget, dict)
                else float(agent_budget)
                for agent_budget in last_log.budget_snapshot.values()
            )
        else:
            total_budget = float(last_log.budget_snapshot)
```

### 修复说明
- **cost和reward**：继续累加（一个月内所有动作的总和）
- **budget**：只取最后一个`StepLog`的`budget_snapshot`总和（表示该月底的实际总预算）

修复后，PNG中显示的预算值将是该月底的实际总预算，而不是所有step_log的预算重复累加。

## 训练结果分析 (2025-10-29)

### 测试条件
- 运行了1个episode的短训
- 启用了调试模式（解锁后只返回解锁动作）
- 配置：`after_month: 15`

### 日志检查结果

**解锁机制验证**：
- ✅ 月份24: `[DEBUG MODE] returning only unlocked actions` - 解锁机制生效
- ✅ 枚举器生成了候选: `points=36, total_candidates=108` - 候选正常生成
- ✅ 从JSON结果看：月份17开始出现动作11（`chosen=[11, 11, 11]`）

**策略过拟合指标**：
- ❌ `entropy_sum=0.0000` - **策略完全确定性，无探索**
- ❌ `logprob_sum=0.0000` - 概率分布过于集中

### JSON结果检查

从训练结果JSON (`v5_0_results_20251029_175901.json`) 观察到：
- **t=15 (月份15)**: `chosen=[3, 3, 5]` - 都是基础动作（可能是在解锁前选择的）
- **t=17 (月份17)**: `chosen=[11, 11, 11]` - **都是解锁动作** ✅
- **t=19, 21, 23, 25, 27**: 都是 `chosen=[11, 11, 11]` - 持续使用解锁动作

### 结论

1. **解锁机制正常工作** ✅
   - 调试模式下，解锁后IND确实切换到了解锁动作（11）
   - 说明代码逻辑是正确的

2. **策略过拟合问题确认** ❌
   - `entropy_sum=0.0000` 表明策略完全确定性
   - 模型在解锁前就学会了固定策略（主要选择动作3、5）
   - 即使解锁机制正常工作，如果模型已经过拟合，可能无法充分探索新动作

3. **月份15的异常**
   - 月份15时仍然使用了基础动作（3, 3, 5）
   - 可能原因：
     - 该动作是在月份14选择但延迟到月份15执行
     - 或者解锁判断的月份编号问题（需要确认）

### 下一步建议

1. **确认解锁机制完全正常**：从结果看解锁机制工作正常
2. **解决策略过拟合问题**：
   - 增加熵奖励系数 (`entropy_coef`)
   - 调整探索温度参数
   - 使用curriculum learning策略
   - 在新动作解锁时增加探索奖励
3. **检查月份编号**：确认月份15时的动作选择时机

## 相关文件

- `envs/v5_0/city_env.py`: 解锁逻辑实现
- `logic/v5_enumeration.py`: 动作枚举和过滤
- `configs/city_config_v5_0.json`: 解锁配置
- `exporters/v5_0/monthly_summary_png.py`: 月度汇总PNG导出（已修复预算计算）
- `verify_unlock_flow.py`: 解锁流程验证脚本
- `check_unlock_execution.py`: 解锁执行检查脚本

