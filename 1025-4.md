# 动作多样性优化总结 (2025-01-25)

## 问题背景
初始训练中观察到动作选择缺乏多样性：
- 几乎看不到动作6
- 很多时候只有一种动作被选择
- EDU智能体倾向于只选择动作0（EDU_S）

## 已调整的参数

### 1. 探索参数调整
**文件**: `configs/city_config_v5_0.json`

#### Multi-Action 参数
- `candidate_topP`: `64` → `128` → `256` → `64` (最终回退)
- `temperature`: `2.0` → `3.0` → `5.0` → `2.0` (最终回退)
- `entropy_bonus`: `0.1` → `0.15` → `0.2` → `0.1` (最终回退)
- `stop_bias`: `-0.2` → `-0.1` → `-0.2` (最终回退)

#### MAPPO 探索参数
- `mappo.exploration.temperature`: `2.0` → `3.0` → `5.0` → `2.0` (最终回退)
- `mappo.exploration.anneal_to`: `0.7` → `1.0` → `1.5` → `0.7` (最终回退)
- `mappo.exploration.anneal_steps`: `200000` → `300000` → `200000` (最终回退)
- `mappo.ppo.entropy_coef`: `0.1` → `0.15` → `0.2` → `0.1` (最终回退)

### 2. Size Bonus 奖励调整
**目标**: 平衡不同动作的吸引力，促进多样性

#### 第一轮调整（过度修正）
- EDU_M (动作1): `1000` → `2000`
- EDU_L (动作2): `2000` → `4000`
- **结果**: EDU智能体只选择动作2

#### 第二轮调整（反向修正）
- EDU_M (动作1): `2000` → `500`
- EDU_L (动作2): `4000` → `1000`
- IND_L (动作5): `2000` → `4000`
- **结果**: EDU智能体只选择动作0

#### 第三轮调整（当前配置）
- EDU_M (动作1): `500` → `1500`
- EDU_L (动作2): `1000` → `3000`
- IND_L (动作5): `4000` → `3000`

### 3. 建筑等级限制配置
**文件**: `logic/v5_enumeration.py` + `configs/city_config_v5_0.json`

#### 新增配置
```json
"building_level_restriction": {
    "enabled": true,
    "apply_to_agents": ["IND"]
}
```

#### 实现逻辑
- 修改 `_get_valid_types_for_point()` 和 `_enumerate_positions()`
- 只对IND智能体应用建筑等级限制
- 允许EDU智能体在所有槽位上建造

### 4. 温度退火机制
**文件**: `trainers/v5_0/ppo_trainer.py`

#### 实现功能
- 线性温度退火：从初始温度逐步降低到目标温度
- 配置参数：`anneal_to`, `anneal_steps`
- 动态更新：每个episode开始时更新温度

#### 关键代码
```python
def _compute_current_temperature(self) -> float:
    progress = min(self.total_steps / self.anneal_steps, 1.0)
    return self.initial_temperature + progress * (self.final_temperature - self.initial_temperature)
```

### 5. 邻近性奖励禁用
**文件**: `configs/city_config_v5_0.json`

#### 原因
- 当前邻近性计算使用随机坐标，无法提供有效奖励信号
- 避免随机干扰影响训练

#### 配置变更
```json
"components": {
    "proximity": false  // 从 true 改为 false
}
```

## 当前配置状态

### Size Bonus 最终值
- 动作0 (EDU_S): `0`
- 动作1 (EDU_M): `1500`
- 动作2 (EDU_L): `3000`
- 动作3 (IND_S): `0`
- 动作4 (IND_M): `1000`
- 动作5 (IND_L): `3000`
- 动作6 (COUNCIL_A): `0`
- 动作7 (COUNCIL_B): `1000`
- 动作8 (COUNCIL_C): `2000`

### 理论收益对比（月净收益 + size_bonus）
- 动作0 (EDU_S): ~95
- 动作1 (EDU_M): ~2685
- 动作2 (EDU_L): ~5300
- 动作5 (IND_L): ~3150
- 动作8 (COUNCIL_C): ~3030

## 经验教训

1. **参数调整需要渐进式**：大幅调整容易导致过拟合到单一动作
2. **Size Bonus 影响巨大**：直接决定动作的相对吸引力
3. **建筑等级限制的重要性**：影响动作的可用性，进而影响选择
4. **邻近性机制需要真实坐标**：当前实现无法提供有效信号
5. **探索参数需要平衡**：过高会导致不稳定，过低会导致缺乏多样性

## 下一步建议

1. **监控训练结果**：观察当前配置下的动作分布
2. **邻近性机制修复**：接入真实槽位坐标，重新启用邻近性奖励
3. **动态奖励调整**：根据训练进度动态调整size_bonus
4. **多样性奖励**：考虑添加显式的多样性奖励机制




