# 1031-5.md：添加数据源一致性检查日志

## 改动目的

为了检查`cand.meta["slots"]`与`cand_idx.point_to_slots`的一致性，添加日志记录实际的数据值。

## 改动内容

### 1. logic/v5_enumeration.py（第279行附近）

**添加日志：记录`cand.meta["slots"]`的值和对应的`point_id`**

```python
# 在创建meta字典之后添加
try:
    self.logger.warning(f"[CAND_META_SLOTS] agent={agent} action_id={action_id} point_id={point_id} point_idx={p_idx} slots={point_info['slots']}")
except Exception:
    pass
```

**作用：**
- 记录每个候选的`cand.meta["slots"]`值
- 记录对应的`point_id`和`point_idx`
- 用于后续对比分析

### 2. envs/v5_0/city_env.py（第614-628行，EXEC标记）

**添加日志：记录EXEC标记时使用的数据源并对比**

```python
# 在标记槽位之前添加
slots_from_meta = cand.meta.get("slots", [])
point_id_from_meta = cand.meta.get("point_id", None)
slots_from_idx = []

# 尝试从cand_idx获取slots
if agent in self._last_cand_idx and self._last_cand_idx[agent]:
    cand_idx = self._last_cand_idx[agent]
    if hasattr(atomic_action, 'point') and atomic_action.point < len(cand_idx.points):
        point_id_from_action = cand_idx.points[atomic_action.point]
        slots_from_idx = cand_idx.point_to_slots.get(point_id_from_action, [])
        try:
            self.logger.warning(f"[EXEC_SLOTS] agent={agent} action_id={action_id} month={self.current_month} point={atomic_action.point} point_id_from_action={point_id_from_action} point_id_from_meta={point_id_from_meta} slots_from_meta={slots_from_meta} slots_from_idx={slots_from_idx}")
        except Exception:
            pass
```

**作用：**
- 记录EXEC标记时使用的`cand.meta["slots"]`
- 记录从`cand_idx.point_to_slots`获取的`slots`
- 对比两者是否一致
- 记录`point_id_from_meta`和`point_id_from_action`是否一致

### 3. envs/v5_0/city_env.py（第873-888行，slot_positions构建）

**添加日志：记录`slot_positions`构建时使用的数据源**

```python
# 在获取slots之后添加
action_id = action.meta.get('action_id', action.atype if hasattr(action, 'atype') else -1)
try:
    self.logger.warning(f"[SLOT_POS_BUILD] agent={agent} action_id={action_id} point={action.point} point_id={point_id} slots={slots}")
except Exception:
    pass
```

**作用：**
- 记录`slot_positions`构建时使用的`cand_idx.point_to_slots[point_id]`
- 记录对应的`point`和`point_id`
- 用于后续对比分析

## 日志标签

### [CAND_META_SLOTS]
- **位置**：`logic/v5_enumeration.py`第279行附近
- **内容**：记录候选生成时`cand.meta["slots"]`的值
- **字段**：
  - `agent`：智能体名称
  - `action_id`：动作ID
  - `point_id`：点ID
  - `point_idx`：点索引
  - `slots`：槽位列表

### [EXEC_SLOTS]
- **位置**：`envs/v5_0/city_env.py`第615行附近（EXEC标记）
- **内容**：记录EXEC标记时使用的数据源并对比
- **字段**：
  - `agent`：智能体名称
  - `action_id`：动作ID
  - `month`：月份
  - `point`：动作的point索引
  - `point_id_from_action`：从`action.point`获取的point_id
  - `point_id_from_meta`：从`cand.meta`获取的point_id
  - `slots_from_meta`：从`cand.meta["slots"]`获取的槽位列表
  - `slots_from_idx`：从`cand_idx.point_to_slots[point_id]`获取的槽位列表

### [SLOT_POS_BUILD]
- **位置**：`envs/v5_0/city_env.py`第876行附近（slot_positions构建）
- **内容**：记录`slot_positions`构建时使用的数据源
- **字段**：
  - `agent`：智能体名称
  - `action_id`：动作ID
  - `point`：动作的point索引
  - `point_id`：点ID
  - `slots`：槽位列表（来自`cand_idx.point_to_slots[point_id]`）

## 检查方案

### 步骤1：运行一次训练

运行训练，生成包含上述日志的记录。

### 步骤2：分析日志

对于31个未执行的槽位中的每一个：

1. **查找`[CAND_META_SLOTS]`日志**：
   - 找到该槽位在候选生成时的`cand.meta["slots"]`值
   - 记录对应的`point_id`和`action_id`

2. **查找`[EXEC_SLOTS]`日志**：
   - 找到EXEC标记时的`slots_from_meta`和`slots_from_idx`
   - 对比两者是否一致
   - 对比`point_id_from_meta`和`point_id_from_action`是否一致

3. **查找`[SLOT_POS_BUILD]`日志**：
   - 找到`slot_positions`构建时的`slots`值
   - 对比是否与`slots_from_meta`或`slots_from_idx`一致

### 步骤3：确定问题原因

根据日志分析结果，确定：
- `cand.meta["slots"]`与`cand_idx.point_to_slots[point_id]`是否一致
- `point_id_from_meta`与`point_id_from_action`是否一致
- 如果不一致，在哪里出现了不一致

### 步骤4：制定修复方案

根据问题原因，制定相应的修复方案。

## 预期结果

通过这些日志，我们可以：
1. 确定31个未执行槽位的具体情况
2. 找出`cand.meta["slots"]`与`cand_idx.point_to_slots`不一致的原因
3. 制定针对性的修复方案

## 注意事项

1. 所有日志使用`WARNING`级别，确保输出
2. 所有日志都使用`try/except`包裹，避免日志记录失败影响主流程
3. 日志字段使用统一的命名规范，便于后续分析

