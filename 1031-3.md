### 1031-3 修复"标记占用使用原始sequence而非processed_sequence"导致空槽位被误占

#### 问题背景
- 从export文件统计：实际执行了175个点（172个唯一槽位）
- 从日志统计：203个槽位被标记为占用（occupied_ids=203）
- **差值：203 - 175 = 28个槽位被标记但未执行**

#### 问题根源

在 `step_phase` 方法中存在逻辑错误：

**执行流程：**
1. **第1542行**：`processed_sequence = self._apply_middleware(agent, sequence)` - 中间件过滤
   - 原始 `sequence`：可能包含6个动作
   - 过滤后 `processed_sequence`：可能只剩3个动作（中间件过滤掉了3个）

2. **第1548行**：`_execute_agent_sequence(agent, processed_sequence)` - 执行动作
   - 只执行 `processed_sequence` 中的动作（3个）

3. **第1554行**：`_update_occupied_slots_for_agent(agent, sequence)` - 标记占用
   - **问题所在**：使用的是原始 `sequence`（6个动作），而不是 `processed_sequence`（3个动作）
   - 导致被中间件过滤掉的动作的槽位也被标记为占用

**导致的问题：**
- 被中间件过滤掉的动作的槽位被标记为占用，但从未实际执行
- 这些槽位后续无法被选择，导致可用槽位减少
- 累积起来，50个月 × 平均每月约0.5个被误标记的槽位 ≈ 28个槽位

#### 代码改动

**文件**：`envs/v5_0/city_env.py`

**位置**：`step_phase` 方法，第1554行

**变更前：**
```python
# 修复：立即更新槽位占用状态，供后续智能体使用
self._update_occupied_slots_for_agent(agent, sequence)  # ❌ 使用原始sequence
```

**变更后：**
```python
# 修复：立即更新槽位占用状态，供后续智能体使用
self._update_occupied_slots_for_agent(agent, processed_sequence)  # ✅ 使用过滤后的sequence
```

#### 修复原理

**确保标记占用时使用的序列与执行时使用的序列一致：**
- 执行时使用 `processed_sequence`（第1548行）
- 标记占用时也应该使用 `processed_sequence`（第1554行）
- 只有实际执行的槽位才会被标记为占用
- 被中间件过滤掉的槽位不会被错误标记，可以在后续月份被选择

**与step方法的对比：**
- `step` 方法（第313行）：已经正确使用了 `processed_sequence` 标记占用
- `step_phase` 方法（第1554行）：当前错误地使用了原始 `sequence`
- 修复后，两个方法的逻辑一致

#### 预期效果

**修复前：**
- `occupied_ids = 203`（包含被中间件过滤掉的槽位）
- 实际执行 = 175个点（172个唯一槽位）
- 差值 = 28个槽位被误标记

**修复后：**
- `occupied_ids` 应该与实际执行的槽位数一致（或接近）
- `non_exec_ids`（未执行但占用）应该显著减少
- 后续月份应该能够正常选择被中间件过滤掉的槽位
- 验证：重新运行训练后，`occupied_ids` - 实际执行点数应该接近0

#### 相关日志

- `[OCCUPY_FROM_SEQUENCE]`：标记来自序列阶段的占用（第794行）
- `[OCCUPY_FROM_EXEC]`：标记来自执行阶段的占用（第626行）
- `[OCCUPIED_SUMMARY]`：月度汇总，显示 `occupied_ids`、`built_ids`、`non_exec_ids`（第1681行）

#### 测试方案

1. **运行训练后检查日志**：
   - 查看 `[OCCUPIED_SUMMARY]` 中的 `occupied_ids` 和 `built_ids`
   - 对比 `occupied_ids` 与实际执行的点数（从export文件统计）
   - 差值应该接近0

2. **检查中间件过滤情况**：
   - 查看中间件日志，确认有多少动作被过滤
   - 确认被过滤的动作的槽位不再被标记为占用

3. **检查后续月份的选择**：
   - 确认之前被误标记的槽位现在可以被正常选择
   - 确认agent在43月后能够继续选择动作

#### 测试结果（修复后）

**日志分析：**

**1. SEQUENCE和EXEC的差值（修复后）：**
- SEQUENCE阶段标记：**199个唯一槽位**
- EXEC阶段标记：**85个唯一槽位**
- 差值：**118个槽位**被SEQUENCE标记但未被EXEC标记
- **即使修复后，仍然有118个槽位被标记但未执行**

**2. OCCUPIED_SUMMARY统计（修复后）：**
- month=41: `occupied_ids=202`, `built_ids=41`, `non_exec_ids=161`
- month=42: `occupied_ids=203`, `built_ids=42`, `non_exec_ids=161`
- month=43: `occupied_ids=203`, `built_ids=42`, `non_exec_ids=161`
- **non_exec_ids仍然很大（161个），没有明显改善**

**3. 从第41个月开始，agent没有可用点：**
- month=41: EDU - `[ENUM_NO_CANDIDATES] reason=no_available_points`
- month=42: IND - `[ENUM_NO_CANDIDATES] reason=no_available_points`
- month=43: EDU, COUNCIL - `[ENUM_NO_CANDIDATES] reason=no_available_points`
- 多个agent出现 `[PIPELINE_SKIP] reason=no_sequence`

**4. Export文件统计：**
- 总点数：177个点
- 唯一slot数量：**172个槽位**
- 从第41个月开始，export文件都是空的（0行，0个点）

#### 结论

**核心问题（用户关注的）：**
- `occupied_ids = 203`（所有被标记为占用的槽位总数）
- 实际执行的点数 = **175个点**
- **差值 = 203 - 175 = 28个槽位** ← **这才是要解决的问题**

**修复"使用processed_sequence标记占用"后：**
- 这个改动不是问题核心
- 需要重新分析这28个槽位是如何产生的

**错误分析（我之前纠结的）：**
- SEQUENCE和EXEC的差值（118个、161个等）是**按槽位去重后的统计**
- 这些差值包含了正常的多月份累计，不是核心问题
- 不应该反复纠结这些统计数字

**下一步（聚焦核心问题）：**
需要找出这28个槽位具体是哪些，以及它们在哪里被标记但未执行：
1. 这28个槽位是如何被标记到`_global_occupied_slot_ids`的？
2. 为什么这些槽位从未实际执行（不在export文件中）？
3. 在哪里标记的，但执行时没有找到candidate或执行失败了？

#### 备注

- 此问题与1031-2.md中记录的修复尝试相关
- 1031-2.md中的修复被回退，因为问题仍然存在
- 本次修复是根本原因：确保标记占用时使用 `processed_sequence` 而不是原始 `sequence`
- 修复后应该能显著减少 `non_exec_ids` 的数量，并解决43月后无法选择动作的问题
- **但测试发现：修复后从第41个月开始，export文件为空，需要进一步调查**
