# 5.0版本日志工厂使用指南

## 概述

5.0版本引入了**配置驱动的集中式日志工厂**，提供统一的日志管理能力。该系统支持主题开关、采样控制、模块级别设置等高级功能，同时保持与print语句的兼容性。

## 核心架构

### 主要组件

- **`utils/logger_factory.py`**: 日志工厂核心实现
- **配置驱动**: 所有日志行为通过 `city_config_v5_0.json` 控制
- **模块化设计**: 支持不同模块的独立日志级别
- **主题开关**: 精确控制特定类型日志的输出

### 核心函数

```python
# 初始化日志系统
init_logging(config: Dict[str, Any]) -> None

# 获取模块日志器
get_logger(module_name: str) -> logging.Logger

# 主题开关控制
topic_enabled(topic: str) -> bool

# 采样控制
sampling_allows(agent: Optional[str], month: Optional[int], step: Optional[int]) -> bool
```

## 配置结构

### 日志配置示例

```json
"logging": {
    "enabled": true,
    "level": "INFO",
    "format": "plain",
    "sinks": {
        "console": true,
        "file": "${paths.logs_dir}/v5_0.log",
        "jsonl": false,
        "rotate": {
            "enabled": true,
            "max_bytes": 10485760,
            "backup_count": 3
        }
    },
    "modules": {
        "env": "INFO",
        "enumeration": "WARN",
        "trainer": "INFO",
        "exporter": "INFO",
        "reward": "WARN",
        "scheduler": "INFO",
        "budget": "INFO"
    },
    "topics": {
        "candidates": true,
        "river_filter": true,
        "occupied_slots": true,
        "export_coords": true,
        "reward_terms": true,
        "policy_select": true,
        "pipeline_traceback": true,
        "phase_switch": true,
        "budget_flow": true,
        "experience_collection": true,
        "training_step": true,
        "environment_step": true,
        "action_execution": true
    },
    "sampling": {
        "every_n_steps": 1,
        "sample_agents": ["IND", "EDU", "COUNCIL"],
        "sample_months": []
    },
    "export": {
        "strict_slot_positions": true,
        "error_policy": "FAIL_FAST"
    }
}
```

### 配置说明

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `enabled` | 是否启用日志系统 | `true` |
| `level` | 全局日志级别 | `INFO` |
| `sinks.console` | 控制台输出 | `true` |
| `sinks.file` | 文件输出路径 | `${paths.logs_dir}/v5_0.log` |
| `sinks.rotate` | 日志轮转配置 | 10MB，保留3个备份 |
| `modules.*` | 各模块日志级别 | 继承全局级别 |
| `topics.*` | 主题开关 | `false` |
| `sampling.*` | 采样控制 | 每步采样，无限制 |

## 使用方式

### 基本使用

```python
from utils.logger_factory import get_logger, topic_enabled, sampling_allows

# 获取模块日志器
logger = get_logger("your_module_name")

# 基本日志输出
logger.info("这是一条信息日志")
logger.warning("这是一条警告日志")
logger.error("这是一条错误日志")
logger.debug("这是一条调试日志")
```

### 主题控制使用

```python
# 检查主题是否启用
if topic_enabled("policy_select"):
    logger.info("策略选择日志：agent选择了动作")

if topic_enabled("budget_flow"):
    logger.info("预算流动日志：预算发生变化")
```

### 采样控制使用

```python
# 受配置开关与采样控制的日志
if topic_enabled("policy_select") and sampling_allows(agent, month, step):
    logger.info(f"policy_select agent={agent} month={month} action_id={action_id}")
```

## 实际应用示例

### 环境初始化

```python
# envs/v5_0/city_env.py
self.logger.info(f"v5.0 环境初始化完成，agents={self.agents}, total_months={self.total_months}")
```

### 策略选择

```python
# solvers/v5_0/rl_selector.py
if topic_enabled("policy_select") and sampling_allows(agent, getattr(state, 'month', None), None):
    self.logger.info(f"policy_select agent={agent} month={getattr(state, 'month', '?')} action_id={action_id}")
```

### 导出系统

```python
# exporters/v5_0/txt_exporter.py
self.logger.info(f"exported month={month} actions={len(month_logs)} path={month_file}")
```

## 主题系统

### 可用主题

| 主题 | 说明 | 用途 |
|------|------|------|
| `candidates` | 候选动作生成 | 调试候选动作逻辑 |
| `river_filter` | 河流过滤 | 调试河流限制逻辑 |
| `occupied_slots` | 槽位占用 | 调试槽位管理 |
| `export_coords` | 导出坐标 | 调试导出系统 |
| `reward_terms` | 奖励计算 | 调试奖励机制 |
| `policy_select` | 策略选择 | 调试RL选择器 |
| `pipeline_traceback` | 管道异常 | 调试管道错误 |
| `phase_switch` | 阶段切换 | 调试调度器 |
| `budget_flow` | 预算流动 | 调试预算系统 |
| `experience_collection` | 经验收集 | 调试训练数据 |
| `training_step` | 训练步骤 | 调试训练过程 |
| `environment_step` | 环境步骤 | 调试环境执行 |
| `action_execution` | 动作执行 | 调试动作执行 |

### 主题控制示例

```python
# 启用特定主题的日志
if topic_enabled("budget_flow"):
    logger.info(f"[BUDGET_FLOW] {agent}: 持续奖励 +{monthly_reward:.1f}")

if topic_enabled("policy_select"):
    logger.info(f"policy_select agent={agent} action_id={action_id} logp={logprob}")
```

## 采样系统

### 采样控制参数

- **`every_n_steps`**: 每N步采样一次
- **`sample_agents`**: 只记录指定智能体的日志
- **`sample_months`**: 只记录指定月份的日志

### 采样使用示例

```python
# 只记录IND、EDU、COUNCIL智能体的日志
if sampling_allows(agent, month, step):
    logger.info(f"Agent {agent} executed action in month {month}")

# 每10步采样一次
if sampling_allows(None, None, step) and step % 10 == 0:
    logger.info(f"Step {step} summary")
```

## 与print语句的关系

### 兼容性设计

5.0版本**没有禁用print语句**，两者可以并存使用：

- **日志系统**: 用于结构化、可配置的日志输出
- **print语句**: 用于即时调试和状态显示

### 使用建议

1. **保留关键print**:
   - 系统启动信息
   - 重要的状态变化
   - 错误提示

2. **逐步迁移到日志系统**:
   - 调试信息 → `logger.debug()`
   - 状态信息 → `logger.info()`
   - 错误信息 → `logger.error()`

3. **配置控制**:
   - 通过配置文件控制日志级别
   - 使用主题开关控制特定类型的输出

## 优势特点

### 1. 配置驱动
- 所有日志行为都通过配置文件控制
- 无需修改代码即可调整日志行为
- 支持运行时配置更新

### 2. 主题开关
- 可以精确控制哪些类型的日志需要输出
- 避免日志过多影响性能
- 便于调试特定功能模块

### 3. 采样控制
- 支持按步骤、智能体、月份进行采样
- 避免日志过多
- 保持关键信息的记录

### 4. 模块化级别
- 不同模块可以设置不同的日志级别
- 便于调试特定模块
- 支持细粒度控制

### 5. 自动轮转
- 支持日志文件自动轮转
- 避免文件过大
- 保留历史日志

### 6. 向后兼容
- 与print语句并存
- 不影响现有代码
- 平滑迁移路径

## 最佳实践

### 1. 模块命名规范
```python
# 使用模块名作为logger名称
logger = get_logger("env")           # 环境模块
logger = get_logger("trainer")       # 训练器模块
logger = get_logger("exporter")      # 导出模块
```

### 2. 日志级别使用
```python
logger.debug("详细的调试信息")      # 开发调试
logger.info("一般信息")             # 正常运行信息
logger.warning("警告信息")          # 潜在问题
logger.error("错误信息")            # 错误情况
```

### 3. 主题控制
```python
# 使用主题开关控制特定日志
if topic_enabled("policy_select"):
    logger.info("策略选择相关信息")

if topic_enabled("budget_flow"):
    logger.info("预算流动相关信息")
```

### 4. 采样控制
```python
# 使用采样控制避免日志过多
if sampling_allows(agent, month, step):
    logger.info(f"Agent {agent} action in month {month}")
```

## 故障排除

### 常见问题

1. **日志不输出**
   - 检查 `logging.enabled` 是否为 `true`
   - 检查日志级别设置
   - 检查主题开关状态

2. **日志文件不创建**
   - 检查 `paths.logs_dir` 配置
   - 检查文件路径权限
   - 检查磁盘空间

3. **日志轮转不工作**
   - 检查 `rotate.enabled` 配置
   - 检查 `max_bytes` 设置
   - 检查文件权限

### 调试技巧

1. **启用所有主题**
```json
"topics": {
    "candidates": true,
    "river_filter": true,
    "occupied_slots": true,
    "export_coords": true,
    "reward_terms": true,
    "policy_select": true,
    "pipeline_traceback": true,
    "phase_switch": true,
    "budget_flow": true,
    "experience_collection": true,
    "training_step": true,
    "environment_step": true,
    "action_execution": true
}
```

2. **设置调试级别**
```json
"level": "DEBUG",
"modules": {
    "env": "DEBUG",
    "trainer": "DEBUG",
    "exporter": "DEBUG"
}
```

3. **禁用采样**
```json
"sampling": {
    "every_n_steps": 1,
    "sample_agents": [],
    "sample_months": []
}
```

## 总结

5.0版本的日志工厂提供了强大而灵活的日志管理能力，通过配置驱动的方式实现了：

- **统一的日志管理**
- **精确的主题控制**
- **灵活的采样机制**
- **模块化的级别设置**
- **自动的日志轮转**
- **与print语句的兼容性**

这种设计既满足了生产环境的需求，又保持了开发调试的便利性，是5.0版本架构设计的重要组成部分。



