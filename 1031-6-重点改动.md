# 1031-6.md：修复EXEC阶段标记错误槽位的问题

## 问题描述

**问题2：EXEC阶段标记了错误的槽位（26个槽位）**

### 问题现象
- EXEC阶段使用`cand.meta["slots"]`标记槽位
- 但`cand.meta["slots"]`来自候选生成时的旧记录
- 实际执行的`action.point`指向不同的槽位
- 导致EXEC标记了错误的槽位

### 典型例子
- 候选生成时：`action_id=1`对应`point_id=slot_0`，`cand.meta["slots"]=['slot_0']`
- 动作选择时：Agent选择了`action_id=1`，但`action.point=1`对应`point_id=slot_12`
- EXEC执行时：
  - 从候选快照查找`cand`，使用的是`action_id=1`
  - `cand.meta["slots"]`是`['slot_0']`（旧的）
  - 但实际应该标记`slot_12`（新的）
- 结果：EXEC标记了`slot_0`，但实际执行的是`slot_12`，导致`slot_0`被标记但没有占用

## 修复方案

### 修复位置
- **文件**：`envs/v5_0/city_env.py`
- **位置**：`_execute_action_atomic`方法（第614-631行附近）

### 修复内容

#### 修改前
```python
# 使用cand.meta["slots"]标记槽位（来自候选生成时的旧记录）
for slot_id in slots_from_meta:
    self._mark_slot_id_occupied(slot_id)
```

#### 修改后
```python
# 修复：优先使用slots_from_idx（来自action.point），而不是slots_from_meta（来自cand.meta）
# 这样可以确保EXEC标记的槽位与实际执行的槽位一致，与slot_positions构建使用同一数据源
if slots_from_idx:
    # 优先使用slots_from_idx（从cand_idx.point_to_slots获取，与slot_positions构建一致）
    slots_to_mark = slots_from_idx
else:
    # 回退到使用slots_from_meta（如果没有cand_idx，比如单动作模式）
    slots_to_mark = slots_from_meta

for slot_id in slots_to_mark:
    self._mark_slot_id_occupied(slot_id)
```

### 修复逻辑

1. **优先使用`slots_from_idx`**：
   - 从`cand_idx.point_to_slots[point_id_from_action]`获取槽位
   - 这些槽位是基于实际执行的`action.point`获取的
   - 与`slot_positions`构建使用同一数据源，确保一致性

2. **回退到`slots_from_meta`**：
   - 如果没有`cand_idx`（比如单动作模式），使用原有的`slots_from_meta`
   - 保持向后兼容性

## 修复效果

### 预期效果
1. **26个槽位问题应该解决**：
   - EXEC标记的槽位会与实际执行的槽位一致
   - 不再出现"EXEC标记了错误的槽位"的情况

2. **EXEC标记与slot_positions构建一致**：
   - 都使用`cand_idx.point_to_slots[point_id_from_action]`获取槽位
   - 确保标记和export使用同一数据源

3. **解决"为什么有槽位被标记但没有占用"的问题**：
   - 至少解决26个槽位的问题（只有EXEC标记的8个槽位 + 两者都有的18个槽位中的EXEC部分）

### 影响的槽位
- **只有EXEC标记的槽位（8个）**：`slot_65`, `slot_66`, `slot_2`, `slot_68`, `slot_3`, `slot_4`, `slot_5`, `slot_71`
- **两者都有的槽位（18个）**：这些槽位的EXEC标记部分会被修复

## 注意事项

1. **保持向后兼容**：
   - 如果没有`cand_idx`（单动作模式），仍然使用`slots_from_meta`
   - 不会影响现有的单动作模式逻辑

2. **与slot_positions构建一致**：
   - 修复后，EXEC标记使用的数据源与`slot_positions`构建使用的数据源一致
   - 都使用`cand_idx.point_to_slots[point_id_from_action]`

3. **日志记录**：
   - 已有的`[EXEC_SLOTS]`日志会记录`slots_from_meta`和`slots_from_idx`
   - 修复后，EXEC标记会优先使用`slots_from_idx`

## 测试建议

修复后需要验证：
1. **运行训练，检查26个槽位是否仍然缺失**：
   - 特别是只有EXEC标记的8个槽位
   - 以及两者都有的18个槽位中的EXEC部分

2. **检查日志中的`[EXEC_SLOTS]`**：
   - 确认`slots_from_idx`和`slots_from_meta`的一致性
   - 修复后，EXEC标记应该使用`slots_from_idx`

3. **检查`[OCCUPY_FROM_EXEC]`和`[SLOT_POS_BUILD]`**：
   - 确认EXEC标记的槽位与export中的槽位一致

## 后续工作

修复问题2后，还需要修复问题1：
- **问题1**：SEQUENCE阶段标记了，但EXEC阶段没有执行（5个槽位）
- 修复后，31个槽位问题应该都能解决

