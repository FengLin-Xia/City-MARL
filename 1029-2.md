# 1029-2 调试与分析纪要（2025-10-29）

## 一、导出与可视化
- 月度PNG预算修复已生效：预算取当月最后一个 `StepLog` 的 `budget_snapshot` 汇总，而非逐步累加。
- 日志片段显示每月汇总正确输出（示例：月17 `budget=227419.2` 等），与 `export_month_XX.txt`、`monthly_summary` 一致。

## 二、解锁机制与训练行为
- 解锁机制验证：调试模式下（解锁后仅返回 9/10/11），IND 于月份16起日志显示“[DEBUG MODE] returning only unlocked actions”，并在结果JSON中月份17起出现 `chosen=[11, 11, 11]`。
- 结论：解锁/Hub3 激活逻辑正常；IND 在解锁后已切换到解锁动作。

## 三、过拟合/策略坍缩迹象
- 指标：`entropy_sum=0.0000`、`logprob_sum=0.0000`（多次出现）。
- 含义：策略完全确定性、概率分布过于集中，探索几乎为零。
- 可能原因（优先级）：
  - 探索项设置：熵系数偏低或衰减过快、温度过低、KL约束过严。
  - 候选空间被过度约束：中间件/合法性/Hub3-only/河流/多动作去重等叠加导致“每步只剩1个可选”。
  - 奖励尺度失衡：个别项过大或裁剪/缩放异常，优势高度偏斜。
  - 训练细节：误用贪心/评估模式、学习率/归一化不稳使logits饱和、topP/stop_bias形成固定模板。
  - 观测/归一化：特征方差过小或统计失效，导致单一映射。
- 建议的无改动核查：
  - 统计每步“可用位点/可行动作数”分布；
  - 观察 entropy/KL/policy-loss/value-loss 曲线是否快速贴底或被硬顶；
  - 检查优势/奖励分布是否极端偏斜；
  - 确认训练阶段确为采样而非argmax；
  - 校验温度与熵系数的实际值及其调度；
  - 审核多动作参数（candidate_topP、dup_policy、stop_bias）是否导致模板化；
  - 检查观测规范化统计是否有效、输入是否非零方差。

## 四、结果文件与日志位置
- `outputs/results/v5_0_results_YYYYMMDD_HHMMSS.json`：
  - 包含 `success/phase`、`training_result.data`（基本信息）、`step_logs`（字符串化的 StepLog：agent、chosen、reward_terms、budget_snapshot、slot_positions）、`env_states`（月序等）。
  - 不包含训练超参数快照（如 lr、entropy_coef、target_kl、num_envs、updates_per_iter）。
- `logs/v5_0.log`：
  - 含训练运行期的关键指标与事件（如 `entropy_sum`、候选统计、解锁调试信息、导出流水）。
- `outputs/export_month_XX.txt`：
  - 每月动作明细文本（坐标与角度）。

## 五、模型检查点（Checkpoint）
- 配置存在：`checkpointing.save_best_metric=eval/return_mean, save_every_steps=10000, max_to_keep=5`。
- 实际文件：`checkpoints/` 中当前可见 `v5_0_ppo_episode_2.pth`、`v5_0_ppo_episode_4.pth`（10/21 生成）。
- 本次短训未产生新checkpoint：可能因步数未达 `save_every_steps`，或未触发基于评估指标的“最佳模型”保存。

## 六、结论与后续
- 解锁链路正常，月度导出修复生效。
- 现象核心为策略过早收敛/探索不足（熵为0）。
- 后续若需持续留存训练超参，建议在训练启动时落盘配置快照到 `outputs/run_config_snapshot.json` 或将超参写入结果JSON的 `training_result.meta`（需改动后再执行）。

