### 1031-1 修复“未执行也被占用”的占位逻辑

#### 问题背景
- 现象：43月后所有agent停止动作，日志显示 `[ENUM_NO_CANDIDATES] ... reason=no_available_points`
- 根因分析：
  - `[OCCUPIED_SUMMARY]` 显示：`month=42 occupied_ids=203 built_ids=42 non_exec_ids=161`
  - 说明：203个槽位全被标记占用，但只有42个真正执行了，161个是“选中但未执行”的误占位
  - 这161个误占位导致可用池被耗尽，后续月份无法枚举到任何候选

#### 修复方案
**核心思路**：区分“序列阶段占用”和“执行期占用”
- **序列阶段**：只写入临时占用集（用于同回合去重），不写入全局集
- **执行期**：真正执行成功后才写入全局集（永久占用）
- **月末清理**：清空临时占用集（未执行的不算占用）

#### 代码改动（`envs/v5_0/city_env.py`）

1. **新增临时占用集**
   - 位置：`__init__` 中初始化集合
   - 变更：添加 `self._reserved_slot_ids_this_month: Set[str] = set()`
   - 作用：存储本回合序列阶段选中的槽位（待执行确认）

2. **修改占位检查逻辑**
   - 位置：`_is_slot_id_occupied` 方法
   - 变更：同时检查全局集和临时集
   - 代码：`return slot_id in self._global_occupied_slot_ids or slot_id in self._reserved_slot_ids_this_month`
   - 作用：枚举候选时，已占用的（包括临时占用）都被过滤

3. **修改序列阶段占位逻辑**
   - 位置：`_update_occupied_slots_for_agent` 方法
   - 变更：
     - 旧：直接调用 `self._mark_slot_id_occupied(slot_id)` 写入全局集
     - 新：只写入 `self._reserved_slot_ids_this_month.add(slot_id)`，不写入全局集
   - 日志：改为 `[OCCUPY_FROM_SEQUENCE] ... (temp_reserved)`

4. **修改执行期占位逻辑**
   - 位置：`_execute_action_atomic` 和 `_execute_action_legacy` 方法
   - 变更：
     - 执行成功后才调用 `self._mark_slot_id_occupied(slot_id)` 写入全局集
     - 执行后从临时集中移除：`self._reserved_slot_ids_this_month.discard(slot_id)`
   - 作用：只有真正落地的槽位才永久占用

5. **月末清理临时集**
   - 位置：`advance_month` 方法
   - 变更：在月份推进时清空 `self._reserved_slot_ids_this_month.clear()`
   - 作用：未执行的临时占用不会跨月，避免误占位累积

6. **重置时清空临时集**
   - 位置：`reset` 方法
   - 变更：在环境重置时也清空 `self._reserved_slot_ids_this_month.clear()`

#### 预期效果
- **`[OCCUPIED_SUMMARY]`**：`non_exec_ids` 应接近0（只有极少数中间件拦截的情况）
- **`[ENUM_STAGE_COUNTS]`**：43月后 `initial` 不再为0，有可用槽位
- **动作恢复**：43-50月应重新出现动作选择和执行

#### 验证结果（2025-10-31 15:30）
✅ **修复成功**：
- `[OCCUPIED_SUMMARY]`：43-50月全部 `non_exec_ids=0`，`occupied_ids` 与 `built_ids` 基本相等
- `[ENUM_STAGE_COUNTS]`：43-50月 `initial=148-156`（不再是0），过滤后仍有候选（`after_river=72-156`）
- `[ENUM_NO_CANDIDATES]`：43-50月无匹配（无候选错误已消失）
- **动作恢复**：终端输出显示43-50月所有agent都有动作执行（有cost和reward），不再是"no reward_terms"
- **占位池健康**：43月时只占用44个槽位（而非修复前的203个），仍有约160个可用槽位

#### 注意事项
- 临时占用集只用于同回合去重，不会跨月污染全局集
- 执行成功的槽位会从临时集移除并写入全局集
- 未执行的槽位在月末自动清空，下月可以重新选择

#### 后续修复：防止同一槽位重复执行（2025-10-31 16:00）

**问题发现**：
- 运行后发现同一槽位在同一月份被多次执行（如 `slot_83` 在 month=43 被执行了3次）
- 原因：执行前没有检查槽位是否已被占用，导致已执行的槽位被再次执行

**修复方案**：

1. **执行前检查槽位占用**
   - 文件：`envs/v5_0/city_env.py`
   - 位置1：`_execute_action_atomic` 方法（约第593-601行）
   - 位置2：`_execute_action_legacy` 方法（约第724-732行）
   - 变更：在执行前检查候选的槽位是否已被占用（调用 `_is_slot_id_occupied`）
   - 逻辑：如果槽位已被占用（在全局集或临时集中），跳过执行并记录警告日志 `[EXEC_SKIP]`
   - 插入位置：在获取候选后、调用V5RewardCalculator之前
   - 完整代码片段：
     ```python
     # 获取候选
     cand = self._get_candidate_from_snapshot(agent, action_id)
     if not cand:
         self.logger.warning(f"未找到action_id={action_id}的候选")
         return 0.0, {}
     
     # 修复：执行前检查槽位是否已被占用（包括全局集和临时集）
     slots = cand.meta.get("slots", [])
     occupied_slots_in_cand = [slot_id for slot_id in slots if self._is_slot_id_occupied(slot_id)]
     if occupied_slots_in_cand:
         self.logger.warning(
             f"[EXEC_SKIP] agent={agent} action_id={action_id} month={self.current_month} "
             f"slots={occupied_slots_in_cand} reason=already_occupied"
         )
         return 0.0, {}
     ```
   - **回退方法**：删除这段检查代码即可

2. **序列阶段占位时检查全局集**
   - 文件：`envs/v5_0/city_env.py`
   - 位置：`_update_occupied_slots_for_agent` 方法（约第808-815行）
   - 变更：在写入临时集前，检查槽位是否已在全局集中
   - 逻辑：如果槽位已在全局集中（已执行过），不应该再写入临时集
   - 插入位置：在 `for slot_id in slots:` 循环内，写入临时集之前
   - 完整代码片段：
     ```python
     for slot_id in slots:
         # 修复：检查槽位是否已在全局集中（已执行过的槽位不应该再写入临时集）
         if slot_id in self._global_occupied_slot_ids:
             self.logger.warning(
                 f"[OCCUPY_SKIP] agent={agent} slot_id={slot_id} month={self.current_month} "
                 f"reason=already_in_global_occupied"
             )
             continue
         
         # 更新当前月份的槽位占用（用于当回合去重）
         self.occupied_slots.add(slot_id)
         # ... 后续代码
     ```
   - **回退方法**：删除 `if slot_id in self._global_occupied_slot_ids:` 检查块即可

**预期效果**：
- 同一槽位在同一回合内不会被多次执行
- 执行前会检查并跳过已被占用的槽位
- 日志中出现 `[EXEC_SKIP]` 表示跳过了重复执行
- 日志中出现 `[OCCUPY_SKIP]` 表示跳过了重复占位

**回退说明**：
- 如需回退，按照上述"回退方法"删除对应的检查代码即可
- 回退后，系统会恢复到修复前的行为（允许同一槽位重复执行，但会通过其他机制去重）

#### 关键问题修复：枚举时未包含临时集（2025-10-31 16:30）

**问题发现**：
- 修复后运行发现：**候选点过滤完全失效，agent在同一个槽位上反复执行动作**
- 根因：枚举时传递给枚举器的 `occupied_slots` **没有包含临时集 `_reserved_slot_ids_this_month`**

**问题分析**：

1. **枚举时缺少临时集**：
   - 在 `get_action_candidates` 和 `get_action_candidates_with_index` 方法中（第420行和第468行）
   - 传递给枚举器的 `occupied_slots` 只包含：
     ```python
     global_occupied = self._global_occupied_slot_ids.union(self.occupied_slots)
     ```
   - **缺少** `_reserved_slot_ids_this_month`（临时集）

2. **并发执行时的时序问题**：
   ```
   1. EDU 和 COUNCIL 并发枚举候选（都看不到临时集）
   2. EDU 选择了 slot_A，在序列阶段占位（写入临时集）
   3. COUNCIL 也选择了 slot_A（因为枚举时看不到临时集）
   4. EDU 执行成功，slot_A 写入全局集
   5. COUNCIL 执行前检查，发现 slot_A 已被占用，跳过执行
   6. 但 COUNCIL 的序列中仍然包含 slot_A，导致重复执行
   ```

3. **执行前检查无法完全修复**：
   - 虽然执行前检查能拦截重复执行，但序列已经生成
   - 如果多个agent都选择了同一个槽位，可能导致混乱

**修复方案**：

- 文件：`envs/v5_0/city_env.py`
- 位置1：`get_action_candidates` 方法（约第420行）
- 位置2：`get_action_candidates_with_index` 方法（约第468行）
- 变更：在枚举时将临时集也包含进去
- 修复代码：
  ```python
  # 修复前：
  global_occupied = self._global_occupied_slot_ids.union(self.occupied_slots)
  
  # 修复后：
  global_occupied = self._global_occupied_slot_ids.union(self.occupied_slots).union(self._reserved_slot_ids_this_month)
  ```
- **作用**：枚举时能看到已被临时占用的槽位，避免并发时重复选择

**预期效果**：
- 枚举时包含临时集，避免并发agent选择同一个槽位
- 从根源解决重复执行问题，而不是依赖执行前检查
- 执行前检查仍然保留，作为双重保险

**回退方法**：
- 将 `global_occupied` 的赋值改回：
  ```python
  global_occupied = self._global_occupied_slot_ids.union(self.occupied_slots)
  ```

#### 回退操作（2025-10-31 17:00）

**回退原因**：用户要求回退到第一个修复（"未执行也被占用"的占位逻辑）执行前的状态。

**已回退的改动**：

1. ✅ **删除执行前检查**
   - 位置：`_execute_action_atomic` 和 `_execute_action_legacy` 方法
   - 操作：删除了执行前的槽位占用检查代码（约593-601行和734-742行）

2. ✅ **删除序列阶段检查**
   - 位置：`_update_occupied_slots_for_agent` 方法（约819-825行）
   - 操作：删除了写入临时集前对全局集的检查

3. ✅ **回退枚举时临时集包含**
   - 位置：`get_action_candidates` 和 `get_action_candidates_with_index` 方法（约420行和468行）
   - 操作：将 `global_occupied` 的赋值改回：
     ```python
     global_occupied = self._global_occupied_slot_ids.union(self.occupied_slots)
     ```
   - 移除了 `.union(self._reserved_slot_ids_this_month)`

**当前状态**：
- ✅ 保留了第一个修复的核心机制（临时占用集、序列阶段只写临时集、执行期才写全局集）
- ✅ 已回退所有后续修复的改动
- ✅ 代码已恢复到第一个修复的状态

**注意事项**：
- 回退后，系统恢复到第一个修复完成时的状态
- 临时占用集机制仍然有效，但枚举时不包含临时集，可能存在并发选择同一槽位的问题

#### 完全回退第一个修复（2025-10-31 17:10）

**回退原因**：用户确认第一个修复（"未执行也被占用"的占位逻辑修复）本身就是问题的根源，需要完全回退。

**已回退的改动**：

1. ✅ **删除临时占用集初始化**
   - 位置：`__init__` 方法（约第114行）
   - 操作：删除了 `self._reserved_slot_ids_this_month: Set[str] = set()`

2. ✅ **恢复占位检查逻辑**
   - 位置：`_is_slot_id_occupied` 方法（约第128行）
   - 操作：恢复为只检查全局集
   - 代码：`return slot_id in self._global_occupied_slot_ids`

3. ✅ **删除reset中的临时集清理**
   - 位置：`reset` 方法（约第286行）
   - 操作：删除了 `self._reserved_slot_ids_this_month.clear()`

4. ✅ **删除advance_month中的临时集清理**
   - 位置：`advance_month` 方法（约第361行）
   - 操作：删除了 `self._reserved_slot_ids_this_month.clear()`

5. ✅ **恢复执行期占位逻辑**
   - 位置：`_execute_action_atomic` 和 `_execute_action_legacy` 方法（约第632行和第763行）
   - 操作：删除了 `self._reserved_slot_ids_this_month.discard(slot_id)`
   - 恢复：直接写入全局集，不再从临时集中移除

6. ✅ **恢复序列阶段占位逻辑**
   - 位置：`_update_occupied_slots_for_agent` 方法（约第804行）
   - 操作：
     - 删除了 `self._reserved_slot_ids_this_month.add(slot_id)`
     - 恢复为直接调用 `self._mark_slot_id_occupied(slot_id)` 写入全局集
   - 代码：
     ```python
     # 恢复前：
     self._reserved_slot_ids_this_month.add(slot_id)  # 只写入临时集
     
     # 恢复后：
     self._mark_slot_id_occupied(slot_id)  # 直接写入全局集
     ```

**当前状态**：
- ✅ 已完全回退第一个修复的所有改动
- ✅ 系统恢复到第一个修复之前的状态
- ✅ 序列阶段和执行期都直接写入全局集

**注意事项**：
- 回退后，系统恢复到第一个修复之前的状态
- 序列阶段选中的槽位会立即写入全局集，即使后续被中间件拦截也会被标记为占用
- 这可能导致"未执行也被占用"的问题重新出现

